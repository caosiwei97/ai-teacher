# 开发日志 — 2026-05-08

#### 竞品深度体验 + E2E 测试用例

**事件**：通过 Playwright 系统体验同类苏格拉底教学产品的完整用户流程（新会话 → 诊断 → 知识图谱 → 教学），发现推荐话题卡片、Tab 诊断 UI、语音输入等细节

**产出**：`docs/4-testing/e2e-test-cases.md` — 20 个 E2E 测试用例

---

#### 迭代 001：项目骨架 + Docker 环境

**状态**：✅ 已完成

**事件**：pnpm monorepo + Next.js 15 + Docker（PG/Redis/MinIO），非标准端口避免本地冲突

**决策**：用户要求用 pnpm 替代 npm；`shamefully-hoist=true` 解决 Next.js/Prisma 依赖解析

---

#### 迭代 002+003：种子数据 + 苏格拉底 Agent 核心

**状态**：✅ 已完成

**事件**：Prisma seed 数据 + 5 个 Agent tools + SSE 流式 Chat API，智谱 glm-4-flash 首次跑通苏格拉底对话

**决策**：Web → Worker 跨 workspace import 通过 webpack 构建；.env 通过 symlink 共享

---

#### 迭代 004：Chat UI + 三栏布局

**状态**：✅ 已完成

**事件**：shadcn/ui 初始化 + 左侧会话列表 + 中央对话 + 右侧路线图，`/learn/[sessionId]` 端到端可用

---

#### 迭代 005：知识图谱生成 + 可视化

**状态**：✅ 已完成

**事件**：Roadmap Agent（generateObject + Zod），LLM 生成失败时自动 fallback 到通用 5 节点模板

---

#### 14:30 — Bug 修复：Tailwind CSS 未加载 + Chat API 400

**状态**：✅ 已完成

**事件**：缺 `postcss.config.mjs` + `globals.css` 未 import + Chat API schema 不兼容 AI SDK useChat，三项一并修复

---

#### 14:50 — E2E 测试基础设施搭建

**状态**：✅ 已完成

**事件**：Playwright 配置 + 7 个 E2E 测试用例，全部通过

---

#### 15:30 — UI 设计系统：暖色教育主题

**状态**：✅ 已完成

**事件**：暖色 stone 系 + amber 强调色，重写左侧栏、右侧栏、聊天组件、路线图组件

---

#### 16:00 — 项目级 Agent 协议建立

**状态**：✅ 已完成

**事件**：创建 `AGENTS.md` — session 启动流程、dev-log 规范、文档同步规则、质量门控

---

#### 16:15 — 迭代 006：诊断摸底

**状态**：✅ 已完成

**事件**：Diagnostic Agent（generateObject + Zod）+ 前端诊断 UI（选择题+简答题）+ 起始节点定位

---

#### 18:00 — 迭代 007：掌握度评分 + 门控

**状态**：✅ 已完成

**事件**：Tutor prompt 注入完整节点列表让 LLM 正确调用 assessMastery；masteryScore ≥ 80 自动推进；前端 onFinish 刷新节点状态

---

#### 18:10 — 迭代 008：评估卡片渲染

**状态**：✅ 已完成

**事件**：从 annotations / toolInvocations 提取 generateAssessment 结果，渲染总结+回顾表+核心标签卡片；历史消息从 DB metadata 恢复

---

#### 19:30 — 迭代 010：会话管理 + 历史记录

**状态**：✅ 已完成

**事件**：后端 PATCH/DELETE + 首页改为会话仪表盘 + 左侧栏新建/归档操作

---

#### 20:00 — Agent 引擎优化方案设计

**状态**：✅ 已完成

**事件**：研究 4 个开源 Agent 框架（Vercel Open Agents / Pi / Hermes / OpenClaw），识别当前引擎 6 大问题，设计优化方案，新增迭代 011/012

---

#### 22:00 — 开发体验优化

**状态**：✅ 已完成

**事件**：`pnpm bootstrap` 一键初始化 + `pnpm dev` 并行启动 + 环境自动化脚本

**决策**：`npx only-allow pnpm` 强制 pnpm；Docker Hub 国内网络通过 host.docker.internal 代理解决

---

#### 迭代 014：快问 + AI 建议回复

**状态**：✅ 已完成

**事件**：选中文本"快问"按钮（streamText）+ 输入框灯泡建议回复（generateText）
