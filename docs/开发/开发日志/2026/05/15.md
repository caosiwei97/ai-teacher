# 开发日志 — 2026-05-15

#### 迭代 038 收尾：沙箱多文件工程结构

**状态**：✅ 已完成

**事件**：沙箱多文件工程结构全部功能验证通过。修复文件上传 multipart 格式适配（execd 需要 `metadata` + `file` 两个 part），修复代理响应解析（execd 空 body 返回 `\n`，`JSON.parse` 需先 `.trim()`）。新增 `safeJson()` helper 统一所有代理路由的响应解析。前端 `useSandboxApi.uploadFile` 修复 FormData 格式。新增 E2E 测试 `e2e/sandbox-ide.spec.ts`（文件 CRUD + PTY + 代码执行）。

---

#### 迭代 039：组件流式生成

**状态**：✅ 已完成

**事件**：renderUI blocks 逐个流式渲染。新建 `StreamingBlockParser`（增量 JSON 解析器，char-by-char 检测 block 边界）。Worker 新增 `tool-input-start/delta/available` 事件处理，实时推送 `ui-stream-start` / `ui-block-delta`。前端 hook 增量消费 block，`ui-blocks` 保留为 fallback 兜底。新增 `BlockSkeleton` 骨架组件 + `fadeSlideIn` 动画。

---

#### 0.4.0 版本收尾

**状态**：✅ 已完成

**事件**：迭代 030-039 全部完成。全量 E2E 回归 52/53 测试通过（沙箱超时为已知 flake）。

---

#### 迭代 040：Next.js → Vite + React 迁移

**状态**：✅ 已完成

**事件**：将 `apps/web` 从 Next.js 15 迁移到 Vite 8 + React Router v7。构建时间 ~8s → 1.2s，dev 启动 ~8s → 212ms。App Router 文件路由替换为 react-router createBrowserRouter（3 个页面 + 1 个 layout）。next/font/google 替换为 @fontsource-variable。Next.js API Routes（纯代理）删除，改用 Vite server.proxy。批量删除 50 个文件的 "use client" 指令。50/53 E2E 通过（3 个沙箱超时为已知 flake）。

**决策**：保持 `window.history.replaceState` 的会话切换模式不变 — session-context 通过 react-router 的 `useLocation` 同步 pathname，实际导航由 replaceState 处理，避免不必要的重渲染
