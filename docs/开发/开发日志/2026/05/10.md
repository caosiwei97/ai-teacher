# 开发日志 — 2026-05-10

#### 07:00 — 迭代 023 收尾：运行时验证 + 修复

**状态**：✅ 已完成

**事件**：修复 Prisma .env symlink 缺失、AI SDK v6 默认 Responses API 不兼容智谱（改 `.chat()` 强制 Chat Completions）、E2E content-type 断言；Docker Desktop → OrbStack 迁移

---

#### 09:45 — 迭代 025：右侧栏实时代码编辑器 + pushCode

**状态**：✅ 已完成

**事件**：pushCode Agent 工具 + 右侧栏 Tab 切换（路线图/代码编辑器）+ CodePanel 组件 + SSE code-push 事件 + Java/C++/Go 语言支持

---

#### 10:00 — 迭代 026：Prompt 工程全面优化

**状态**：✅ 已完成

**事件**：assessMastery 从每轮强制改为 2-3 轮后评估；recordStrength/recordMisconception 持久化到 LearnerProfile；教学模式作为核心策略切换（Prisma 新增 teachingMode 字段）；ToolRegistry.toPromptSection() 动态注入

---

#### 21:00 — Bug 修复：Diagnostic API 500 + LLM 切换 DeepSeek

**状态**：✅ 已完成

**事件**：智谱 GLM 的 JSON 被 markdown code fence 包裹导致 generateObject 解析失败；AI SDK 默认发送 `json_schema` 格式智谱/DeepSeek 都不支持

**决策**：切换到 DeepSeek-V4-Flash + `@ai-sdk/deepseek` 原生 provider（原生支持 generateObject）；全局替换 8 处硬编码模型名

---

#### 21:10 — 迭代 027-029 规划

**状态**：✅ 已完成

**事件**：规划 3 个迭代 — 027 聊天内诊断重构、028 SSE 事件扩展、029 掌握报告与自动过渡

---

#### 22:00 — 迭代 027：聊天内诊断重构

**状态**：✅ 已完成

**事件**：诊断从独立 REST API 改为聊天内 askQuestion 工具 + Tab 选择题 UI + hidden 消息；新会话不再立即生成路线图，所有节点初始 not-started；移除旧诊断流程（DiagnosticQuiz 组件、diagnosticState machine）

---

#### 22:30 — E2E Mock 策略：24/24 测试 11 秒

**状态**：✅ 已完成

**事件**：MOCK_LLM=true 环境变量触发服务端 mock（Provider 级 MockLanguageModelV3 + Route 级 quick path）；模型统一 glm-4-flash → glm-5-turbo

---

#### 23:30 — 迭代 024：renderUI + 结构化教学组件

**状态**：✅ 已完成

**事件**：renderUI Agent 工具 + 3 种 UIBlock（table/callout/comparison）+ 3 个渲染组件 + SSE ui-blocks 事件管道打通

---

#### 23:50 — 迭代 028：SSE 事件扩展

**状态**：✅ 已完成

**事件**：新增 roadmap-updated / session-updated SSE 事件，节点掌握后实时推送到前端，右侧栏无需等 onFinish re-fetch

---

#### 23:55 — 迭代 029：掌握报告与自动节点过渡

**状态**：✅ 已完成

**事件**：从手动"下一节"改为 Agent 自动过渡；新增 3 种 UIBlock（Heading/Badge/MasteryReport）；assessMastery 返回 instruction 指导自动桥接
