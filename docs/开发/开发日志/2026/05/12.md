# 开发日志 — 2026-05-12

#### 09:30 — 卡片标题修复 + Prompt Pipe 重构

**状态**：✅ 已完成

**事件**：所有 UI 卡片标题加 truncate + 内联代码渲染；Callout 内容区升级为完整 Markdown；Prompt 系统从单体函数重构为 Pipe 模式（8 个独立 section 函数 + pipe 组合）

---

#### Bug 修复：切换/新建对话页面闪烁

**状态**：✅ 已完成

**事件**：ThreeColumnLayout 在每个页面组件内各自渲染，切换会话时整个三栏布局销毁重建

**决策**：创建 `(app)` 路由组 + SessionContext，三栏布局提升到共享 Layout 层；会话切换用 `window.history.replaceState` + React 状态管理，不触发 Next.js 路由导航

---

#### 16:30 — Bug 修复：学完小主题后不自动进入下一个

**状态**：✅ 已完成

**事件**：generateAssessment 和 advanceNode 的 promptSnippet 与 assessMastery.instruction 冲突，模型看到矛盾指令耗尽 step 预算

**决策**：移除冗余的 generateAssessment/advanceNode 工具（assessMastery 已内部处理节点推进）；stepCountIs(5)→(7) 增加安全余量

---

#### 18:00 — Judge0 → OpenSandbox 迁移

**状态**：✅ 已完成

**事件**：Judge0 在 macOS ARM64/WSL2 无法运行（依赖 cgroups v1），重写 sandbox.ts 为 OpenSandbox 生命周期 API + execd 代码执行 API，修复 4 个 API 兼容性问题

**决策**：选定阿里 OpenSandbox（跨平台、Docker 隔离、多语言支持）

---

#### 22:30 — 3 个 UI/Prompt bug 修复

**状态**：✅ 已完成

**事件**：诊断卡片 Tab 溢出（加横向滚动）+ 诊断完成后消息重复说"定制路线"（prompt 去重）+ 流式渲染未闭合 markdown 标记显示为原始文本（新增 closeUnclosedMarkdown 自动修复）
