# 开发日志 — 2026-05-13

#### 迭代 037：代码编辑器优化 + 沙箱增强

**Bug 修复**：
- 右侧面板折叠后消失：展开按钮响应式类 `lg:hidden` 导致桌面端隐藏，合并为单个按钮
- 编辑器清空后高度塌缩：CodeMirror 容器空内容时高度为 0，加 `min-h-[200px]`

**UI 优化**：
- 运行按钮从底部全宽 → 工具栏 Play icon + Copy 按钮 + 清空纯图标
- 编辑器启用行号 + 当前行高亮 + 语法高亮 + 括号匹配
- 输出区新增 Output 标题 + 执行耗时

**沙箱增强**：创建沙箱时注入 OPENAI_API_KEY + OPENAI_BASE_URL

**诊断增强**：编程主题诊断自动加语言偏好题（方案 A：仅改 prompt）

**开发体验**：`pnpm dev` 保留 .next 缓存秒启动，`pnpm dev:fresh` 清缓存重启

---

#### 文档结构审查与重整

**状态**：✅ 已完成

**事件**：
- 归档：`架构升级方案.md`（迭代 016-022 已实施）、`Agent引擎优化方案.md`（迭代 011-012 已实施）
- 精简：删除 12 个 E2E 详情文件，只保留 README 矩阵总览
- 新增：`错误降级策略.md`、`安全设计.md`、`发版测试清单.md`
- AGENTS.md §3.4 调整：commit 前只需 build，E2E 改为发版时运行

**下一步**：迭代 009（学习资料上传 + RAG）或迭代 038（沙箱多文件工程结构）或产品优化

---

#### 14:00 — 沙箱错误传递 + 编辑器集成终端

**状态**：✅ 已完成

**事件**：
- Bug 修复：沙箱执行报错时 server 直接抛 500，前端只显示"执行失败: 500"无法定位原因。添加 try-catch 将错误详情（message）以 `{stderr, status: "Sandbox Error"}` 形式返回，前端可显示具体错误
- UI 改造：CodePanel 从条件显示输出改为 VSCode 风格集成终端 — 编辑器和终端上下分割，可拖拽调整大小（`react-resizable-panels`），终端始终可见
- 新增 `terminal-panel.tsx`：暗色终端面板，stdout 绿色/stderr 红色/沙箱错误 amber 警告，空闲时显示占位符，执行中显示 spinner

**决策**：沙箱错误走 HTTP 200 而非 500 — 因为前端 `useCodeExec` 对非 200 只显示 `执行失败: {status}`，丢失了错误详情。用 200 + `status: "Sandbox Error"` 让终端面板可以区分展示

---

#### 14:20 — 编辑器终端一体化 + execd 错误解析修复

**状态**：✅ 已完成

**事件**：
- UI 改进：去掉 react-resizable-panels 拖拽分割，改为编辑器和终端共享同一暗色背景、细线分隔的一体化面板（Jupyter cell 风格）
- Bug 修复（根因）：`parseExecdSse` 只解析 `type: "stdout"` 和 `type: "stderr"`，但 OpenSandbox execd 的运行时错误返回 `type: "error"` + `error.traceback[]`，完全被忽略。所有 Python 异常（ImportError、SyntaxError 等）都显示为"无输出"。增加 `type: "error"` 解析，将 traceback 清除 ANSI 转义码后写入 stderr

**决策**：不在沙箱镜像里预装 openai — 用户代码如果 import 失败，现在能正确看到 `ModuleNotFoundError` 而不是空白

---

#### 14:40 — 沙箱执行超时 + 示例代码模型动态化

**状态**：✅ 已完成

**事件**：
- Bug 修复：`execCode` 无超时机制，沙箱代码卡住时前端永远 loading。加了 15 秒 AbortController 超时
- Bug 修复：tutor prompt 中沙箱示例代码硬编码模型名，但用户可能配置不同 provider（Claude、GPT 等）。改为从 `DEFAULT_SANDBOX_MODEL` → `LLM_MODEL` → `"deepseek-v4-flash"` 三级 fallback 动态读取，同时 base_url 也从 env 动态注入

**决策**：不在 prompt 里推断 provider → model 映射（太脆弱），而是让用户通过 `DEFAULT_SANDBOX_MODEL` 环境变量显式配置沙箱示例用的模型名

---

#### 15:10 — 统一模型配置：沙箱复用 + 无配置 guard

**状态**：✅ 已完成

**事件**：
- 沙箱复用用户 LlmConfig：`buildSandboxEnv` 改为接受 `SandboxLlmConfig` 参数，sandbox route 从 DB 读取用户的 `LlmConfig`（解密 apiKey）注入沙箱环境变量，不再只读 .env
- Tutor prompt 动态化：`TutorPromptContext` 新增 `sandboxModel`/`sandboxBaseUrl`，`coreRulesSection` 从 context 读取模型名和 base_url，不再读 env 或硬编码
- 无配置 guard：learn 页面启动时检查 DB 配置 + `GET /api/llm/env-status` 检查 env 配置，两者都没有时 ChatInput 禁用 + 显示"请先配置模型"提示
- useCodeExec 接受可选 `llmConfigId` 参数，CodePanel → RightSidebar → learn page 传递 `selectedConfigId`
- 清理 `DEFAULT_SANDBOX_MODEL` env var（不再需要）

**决策**：统一用户设置页的 LlmConfig 作为唯一配置源，.env 只作为 fallback。沙箱、tutor prompt、聊天三者共享同一套配置
