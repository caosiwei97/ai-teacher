# 开发日志 — 2026-05-09

#### 10:00 — 迭代 011：Agent 引擎重构 Phase 1

**状态**：✅ 已完成

**事件**：统一 Provider 单例 + BaseAgent 抽象基类 + NodeService/MessageService 服务层 + createTutorTools 工厂函数，chat/route.ts 从 293 行降至 166 行

---

#### 11:00 — 迭代 012：Agent 引擎重构 Phase 2

**状态**：✅ 已完成

**事件**：MessageTransformer 消息截断策略 + Token 预算估算 + executeWithRetry（指数退避）+ executeWithFallback（主模型→fallback 链）+ 120s 流式超时

---

#### 16:00 — 迭代 016：架构拆分 Phase 1 — Next.js → Hono

**状态**：✅ 已完成

**事件**：创建独立 Hono API 服务，迁移 8 个 Next.js API 路由，Next.js 变纯前端（删除 app/api/），通过 rewrite proxy 转发

**决策**：用 Next.js rewrite proxy 而非前端跨域调用，避免 CORS + SSE 问题

---

#### 21:00 — 迭代 017：架构拆分 Phase 2 — Worker + BullMQ

**状态**：✅ 已完成

**事件**：Worker 激活为 BullMQ 消费进程，chat-turn 队列：Hono POST 入队 → Worker 消费 → Agent 执行 → Redis Pub/Sub → Hono SSE 转发；前端 chat hook 从 AI SDK useChat 重写为自定义 POST+SSE

**决策**：BullMQ + Redis Pub/Sub 解耦 API 层和 Agent 层；Worker 启动时扫描 processing 消息标记 failed

---

#### 22:00 — 首页重设计 + 护眼配色全局改造

**状态**：✅ 已完成

**事件**：首页改为欢迎页 + 推荐话题（`GET /api/suggested-topics`），全局配色切换为护眼暖色暗色主题

**决策**：护眼配色基于 4000K 暖色温研究（暗色模式最低视觉疲劳）

---

#### 23:40 — 迭代 013：学习者画像 + 跨会话记忆

**状态**：✅ 已完成

**事件**：ProfileService 封装画像更新（去重合并 strengths/weaknesses），PATCH status=completed 时自动触发

---

#### 23:50 — 迭代 019：上下文管理升级

**状态**：✅ 已完成

**事件**：ContextManager 替换朴素截断；AgentMessage 分层类型（llm/diagnostic/mastery/system）；token 超 80% 预算时 LLM 生成 StructuredSummary；Compaction 失败自动 fallback 到截断

---

#### 23:50 — 迭代 020：A2UI 框架 + 代码编辑器

**状态**：✅ 已完成

**事件**：A2UI 块渲染框架（6 种 UIBlock discriminated union）+ CodeMirror 6 编辑器 + KaTeX 公式渲染，替换旧的内联 parseContent

---

#### 23:50 — 迭代 021：代码执行沙箱

**状态**：✅ 已完成

**事件**：Judge0 Docker 配置 + sandbox 客户端 + executeCode ToolDefinition + Hono sandbox 端点 + CodeResultBlock "编辑并重新运行"

**决策**：采用方案 C（Agent 工具 + Hono 端点），不建独立队列。Judge0 执行 ≤10s，在 120s 流式超时内绰绰有余

---

#### 23:00 — 迭代 022：多 Agent 协作 + Subagent

**状态**：✅ 已完成

**事件**：SubagentRegistry + AssessmentAgent（3 步限制）+ ResearchAgent（5 步限制）+ delegateTask 工具，子 Agent 用便宜模型

**决策**：工厂函数闭包模式传递 SubagentRegistry，避免修改核心类型；`toModelOutput()` 过滤子 Agent 输出防上下文污染

---

#### 23:30 — 迭代 023：技术栈全面升级（AI SDK v6 + Zod 4）

**状态**：✅ 已完成

**事件**：5 个子任务并行迁移（packages/shared、agent、worker、server、web），AI SDK codemod 超时改为手动逐包迁移

**决策**：`CoreMessage`→`ModelMessage`、`parameters`→`inputSchema`、`maxSteps`→`stopWhen`、`UIMessage.content`→`.parts`；Zod 3→4 全部 `zod/v3` import 改为 `zod`
