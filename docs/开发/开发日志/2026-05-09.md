# 2026-05-09 开发日志

---

## 10:00 — 迭代 011：Agent 引擎重构 Phase 1

**状态**：✅ 已完成

**事件**：
- 创建统一 Provider（`getProvider()` 单例，消除 3 处重复的 `createOpenAI`）
- 创建 `BaseAgent` 抽象基类（统一 provider 管理、model 获取、fallback）
- 创建 `NodeService` 封装节点操作（掌握度更新、自动推进下一节点）
- 创建 `MessageService` 封装消息持久化（learner + tutor 消息）
- 创建 `createTutorTools()` 工厂函数，工具 execute 函数直接操作数据库（副作用内化）
- TutorAgent / RoadmapAgent / DiagnosticAgent 继承 BaseAgent
- 瘦身 `chat/route.ts` 从 293 行降至 166 行（移除内联 persistChatTurn）
- 修复首页重定向行为导致的 E2E 测试失败
- 修复 Playwright 无法找到 pnpm 的配置问题

**代码变更**：
- `apps/worker/src/agent/provider.ts` — 新建，统一 Provider 单例
- `apps/worker/src/agent/base-agent.ts` — 新建，BaseAgent 抽象基类
- `apps/worker/src/agent/services/node-service.ts` — 新建，节点操作服务
- `apps/worker/src/agent/services/message-service.ts` — 新建，消息持久化服务
- `apps/worker/src/agent/tools/create-tools.ts` — 新建，工具工厂（assessMastery/advanceNode 含 DB 副作用）
- `apps/worker/src/agent/tutor.ts` — 重构为 TutorAgent 类 + 向后兼容导出
- `apps/worker/src/agent/roadmap.ts` — 重构为 RoadmapAgent 类 + 向后兼容导出
- `apps/worker/src/agent/diagnostic.ts` — 重构为 DiagnosticQuestionAgent + DiagnosticEvaluateAgent
- `apps/web/src/app/api/chat/route.ts` — 瘦身，使用 TutorAgent + MessageService
- `e2e/home.spec.ts` — 适配首页重定向行为
- `e2e/session-management.spec.ts` — 适配首页重定向行为
- `playwright.config.ts` — 修复 pnpm 路径问题

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 20/20 E2E 测试通过 ✅

**下一步**：迭代 012（Agent 引擎重构 Phase 2）

---

## 11:00 — 迭代 012：Agent 引擎重构 Phase 2

**状态**：✅ 已完成

**事件**：
- 创建 `context.ts` 实现 MessageTransformer 消息截断策略（保护首尾、压缩中间、单条截断）
- 实现 Token 预算估算（estimateTokens + estimateMessagesTokens）
- BaseAgent 新增 `executeWithRetry()`（指数退避 1s/2s/4s，检测 429/500/503/timeout/ECONNRESET）
- BaseAgent 新增 `executeWithFallback()`（主模型→fallback 模型链）
- TutorAgent 集成 transformMessages 处理输入消息
- RoadmapAgent / DiagnosticAgent 集成 executeWithRetry
- chat/route.ts persistMessages 添加 120s 流式超时检测（Promise.race）

**代码变更**：
- `apps/worker/src/agent/context.ts` — 新建，消息截断 + Token 估算
- `apps/worker/src/agent/base-agent.ts` — 新增 executeWithRetry + executeWithFallback
- `apps/worker/src/agent/tutor.ts` — 集成 transformMessages
- `apps/worker/src/agent/roadmap.ts` — 集成 executeWithRetry
- `apps/worker/src/agent/diagnostic.ts` — 集成 executeWithRetry
- `apps/web/src/app/api/chat/route.ts` — persistMessages 超时检测

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 20/20 E2E 测试通过 ✅

**下一步**：迭代 013（学习者画像 + 跨会话记忆）

---

## 23:40 — 迭代 013：学习者画像 + 跨会话记忆

**状态**：✅ 已完成

**事件**：
- 创建 ProfileService 封装学习者画像更新逻辑（去重合并 strengths/weaknesses，保留最近 10 次会话摘要）
- 在 PATCH status=completed 时自动触发画像更新（从 roadmap 节点数据计算 strengths/weaknesses）
- 验证已有代码在 chat/route.ts 中通过 buildLearnerProfile() 注入画像到 Agent prompt
- 所有构建和 E2E 测试通过

**代码变更**：
- `apps/worker/src/agent/services/profile-service.ts` — 新建，封装画像更新逻辑（upsert + 去重合并）
- `apps/web/src/app/api/sessions/[id]/route.ts` — PATCH handler 新增画像更新触发 + updateProfileAfterCompletion 辅助函数

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 20/20 E2E 测试通过 ✅

**下一步**：迭代 011（Agent 引擎重构 Phase 1）或迭代 009（学习资料上传 + RAG）

---

## 22:00 — 首页重设计 + 护眼配色全局改造

**状态**：✅ 已完成

**事件**：
- 首页从"自动重定向到活跃会话"改为"始终展示欢迎页 + 推荐话题"
- 新增 `GET /api/suggested-topics` 接口（后端配置推荐话题）
- 新增 `WelcomeContent` 组件（问候语 + 6 个推荐话题卡片 + 输入框）
- 全局配色从浅色主题切换为护眼暖色暗色主题（基于 4000-4500K 色温研究）
- `ThreeColumnLayout` 和 `LeftSidebar` 支持无 active session 状态
- 删除死代码 `home-dashboard.tsx` 和 `new-session-form.tsx`
- 安装 `ui-ux-pro-max` 设计技能
- 更新 E2E 测试适配新首页行为

**设计决策**：
- 护眼配色基于西北工业大学和 IST Imaging 的研究：暗色模式 + 4000K 暖色温最低视觉疲劳
- 采用方案 A（输入主题后创建会话），不修改 Prisma schema
- 推荐话题独立为 API 端点，方便后续迁移到数据库配置

**代码变更**：
- `apps/web/src/app/globals.css` — 全局配色改为暖色暗色护眼模式
- `apps/web/src/app/page.tsx` — 首页逻辑重写（不再重定向）
- `apps/web/src/app/welcome-content.tsx` — 新增欢迎组件
- `apps/web/src/app/api/suggested-topics/route.ts` — 新增推荐话题 API
- `apps/web/src/components/layout/three-column.tsx` — currentSessionId/nodes 可选
- `apps/web/src/components/layout/left-sidebar.tsx` — currentSessionId 可选
- `e2e/home.spec.ts` — 更新测试
- 删除 `home-dashboard.tsx`、`new-session-form.tsx`

**验证结果**：
- `pnpm --filter @ai-teacher/web build` 通过 ✅
- LSP diagnostics 零错误 ✅
- Git push 成功 ✅

**下一步**：迭代 009（学习资料上传 + RAG）或产品优化

---

## 16:00 — 迭代 016：架构拆分 Phase 1 — Next.js → Hono API 迁移

**状态**：✅ 已完成

**事件**：
- 创建独立 Hono API 服务 `apps/server/`，迁移所有 8 个 Next.js API 路由
- Next.js 变为纯前端（删除 `app/api/` 目录），通过 rewrite proxy 转发 `/api/*` 到 Hono
- 首页从服务端组件改为客户端组件（移除 prisma 直接调用）
- 恢复 seed 脚本（之前被误删）
- 修复 4 个 E2E 测试（topic 文案不匹配 + 首页不再重定向）

**技术决策**：
- 使用 Next.js rewrite proxy 而非前端直接跨域调用（避免 CORS + SSE 问题）
- 前端继续使用相对 URL `/api/...`，Next.js 代理到 Hono `:38422`
- Worker 空壳保留（Agent 代码被 server 通过相对路径引用）
- 从 web 移除 `@ai-teacher/db`、`@ai-teacher/shared`、`@ai-sdk/openai` 依赖（web 变纯前端）

**代码变更**：
- `apps/server/` — 新建 Hono 项目（12 个文件：entry、middleware、7 个路由）
- `apps/web/next.config.ts` — 添加 `/api/*` → Hono proxy rewrite
- `apps/web/src/app/page.tsx` — 移除 prisma，改为纯客户端组件
- `apps/web/src/app/welcome-content.tsx` — 客户端获取 sessions + topics
- `apps/web/src/app/learn/[sessionId]/page.tsx` — suggest-reply URL 更新
- `apps/web/src/components/chat/quick-question.tsx` — quick-question URL 更新
- `apps/web/src/lib/api-client.ts` — 保持相对 URL（由 proxy 处理）
- `apps/web/src/hooks/use-chat-stream.ts` — 保持相对 URL
- `apps/web/src/app/api/` — 删除全部 8 个 API 路由文件
- `package.json` — `dev:worker` → `dev:server`，恢复 `db:seed`
- `.env` / `.env.example` — 新增 `SERVER_PORT=38422`
- `playwright.config.ts` — 双 webServer（server + web）
- `packages/db/prisma/seed.ts` — 从 git 恢复
- `packages/db/package.json` — 恢复 seed script
- `e2e/home.spec.ts` — topic 文案修正（罗马帝国 → 文艺复兴）
- `e2e/mastery.spec.ts` — 节点状态断言改为检查第一个节点
- `e2e/session-management.spec.ts` — 首页不再重定向，改为点击进入

**验证结果**：
- `pnpm --filter @ai-teacher/server typecheck` → 零错误 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- Hono API 独立启动 → `http://localhost:38422` ✅
- 23/24 E2E 测试通过（1 skip） ✅

**下一步**：迭代 017（架构拆分 Phase 2：Worker + BullMQ）

---

## 21:00 — 迭代 017：架构拆分 Phase 2 — Worker 激活 + BullMQ 队列

**状态**：✅ 已完成

**事件**：
- 激活 Worker 为独立 BullMQ 消费进程（port :38423）
- 实现 chat-turn 队列：Hono POST 入队 → Worker 消费 → Agent 执行 → Redis Pub/Sub 推送 → Hono SSE 转发
- Prisma Message 新增 `status` 字段（sending → processing → completed / failed）
- 前端 chat hook 从 AI SDK `useChat` 重写为自定义 POST + SSE 双请求实现
- 删除 5 个已废弃的 standalone tool 文件 + stale dist/
- 新增 `pnpm dev:worker` 脚本，`pnpm dev` 现在启动三进程并行

**技术决策**：
- BullMQ + Redis Pub/Sub：解耦 API 层和 Agent 层，Worker 可独立扩展
- Worker 崩溃恢复：启动时扫描 processing 状态的消息，标记为 failed
- SSE 事件格式：text-delta / tool-call / tool-result / done / error（JSON over SSE）
- quick-question 和 suggest-reply 暂不走队列（非核心路径）

**代码变更**：
- `packages/db/prisma/schema.prisma` — Message 新增 status 字段（default "completed"）
- `packages/shared/src/schemas/message.ts` — 新增 MessageStatus enum
- `apps/worker/src/index.ts` — 重写为 BullMQ 启动入口 + 崩溃恢复 + 健康检查 HTTP
- `apps/worker/src/processors/chat-turn.ts` — 新增，核心队列消费者
- `apps/worker/src/lib/learner-profile.ts` — 新增，从 server 迁移的 buildLearnerProfile
- `apps/server/src/routes/chat.ts` — 重写为 POST 入队 + GET SSE 双端点
- `apps/server/src/services/queue.ts` — 新增，BullMQ Queue 实例
- `apps/web/src/hooks/use-chat-stream.ts` — 重写为自定义 POST + SSE hook
- `package.json` — 新增 dev:worker 脚本
- `.env.example` + `.env` — 新增 WORKER_PORT=38423
- `playwright.config.ts` — 三 webServer（server + worker + web）
- 删除 `apps/worker/src/agent/tools/` 下 5 个废弃文件 + `apps/worker/dist/`

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/server build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 24/24 E2E 测试通过 ✅

**下一步**：迭代 018（Agent 引擎 Phase 3：StateGraph）

---

#### 2026-05-09 23:50 — 迭代 019：上下文管理升级

**状态**：✅ 已完成

**事件**：
- 实现 ContextManager 类，替换 tutor-graph 中朴素的 `transformMessages()` 截断
- 新增 AgentMessage 分层类型（llm / diagnostic / mastery / system），非 LLM 消息不发给模型
- 实现结构化 Compaction：token 超过预算 80% 时，调用 LLM 生成 StructuredSummary（completedTopics + masteryState + misconceptions + keyDecisions）
- 支持增量摘要更新（已有旧摘要时用 UPDATE prompt 合并）
- Compaction 失败时自动 fallback 到截断模式，不影响正常对话
- Session 新增 `summary Json?` 字段存储摘要

**代码变更**：
- `packages/db/prisma/schema.prisma` — Session 新增 `summary Json?`
- `packages/shared/src/schemas/summary.ts` — 新增 StructuredSummary Zod schema
- `packages/shared/src/index.ts` — 导出 summary schema
- `packages/agent/src/types.ts` — TutorState 新增 `summary?` 字段
- `apps/worker/src/agent/agent-message.ts` — 新增 AgentMessage union type + 类型守卫 + 转换工具
- `apps/worker/src/agent/compaction.ts` — 新增 LLM 摘要生成（generateObject）+ 增量更新 + 格式化
- `apps/worker/src/agent/context-manager.ts` — 新增 ContextManager 类（process/transformContext/compact/estimateAgentTokens）
- `apps/worker/src/graphs/tutor-graph.ts` — prepare_context 节点接入 ContextManager
- `apps/worker/src/processors/chat-turn.ts` — 注入 ContextManager 实例 + summary 加载/保存

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 24/24 E2E 测试通过 ✅

**下一步**：迭代 020（A2UI 框架 + 代码编辑器）

---

## 23:50 — 迭代 020：A2UI 框架 + 代码编辑器

**状态**：✅ 已完成

**事件**：
- 实现 A2UI（AI-to-UI）块渲染框架，将 Agent 输出映射为结构化 UIBlock
- 定义 UIBlock Zod discriminated union schema（6 种类型：text/assessment/quiz/code-result/formula/diagram）
- 安装 CodeMirror 6 + KaTeX 依赖
- 实现 6 个 UIBlock 渲染组件 + Registry + MessageContent 入口
- 实现 CodeMirror 6 代码编辑器组件（暖色自定义主题 + 异步语言加载）
- 集成 MessageContent 到 chat-message.tsx，替换旧的内联 parseContent 渲染
- chat-area.tsx 新增从 annotations 提取 uiBlocks 逻辑
- 向后兼容：无 uiBlocks 的旧消息降级为 TextBlock 渲染

**代码变更**：
- `packages/shared/src/schemas/ui-block.ts` — 新增 UIBlock discriminated union schema
- `packages/shared/src/index.ts` — 导出 ui-block
- `apps/web/src/components/ui-blocks/` — 8 个新文件（index/registry/text-block/assessment-block/quiz-panel/code-result-block/formula-display/diagram-renderer）
- `apps/web/src/components/editor/` — 3 个新文件（code-editor/warm-theme/language-map）
- `apps/web/src/components/chat/chat-message.tsx` — 使用 MessageContent 替换内联渲染
- `apps/web/src/components/chat/chat-area.tsx` — 提取 uiBlocks 并传递给 ChatMessage
- `apps/web/package.json` — 新增 codemirror + katex 依赖

**验证结果**：
- LSP diagnostics 全部零错误 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 24/24 E2E 测试通过（1 个 flaky 重试后通过） ✅

**下一步**：迭代 021（代码执行沙箱）

---

#### 2026-05-09 (3) — 迭代 021：代码执行沙箱

**状态**：🔧 进行中

**方案决策**：
- 采用方案 C：Agent 工具 + Hono 端点，不建 BullMQ code-exec 队列
- 理由：Judge0 执行 ≤10s，在 120s 流式超时内绰绰有余；子 Agent（迭代 022）的 executeCode 也嵌套在 streamText 内同步执行，不需要独立队列
- Rate limiting 用 ToolRegistry.beforeToolCall hook + Redis counter 实现

**事件**：
- 完成 Judge0 Docker Compose 配置（judge0-server + redis-judge0）
- 完成 Worker sandbox 客户端（submitCode + 语言映射）
- 完成 executeCode ToolDefinition（安全检查 + 资源限制 + 自描述 promptSnippet）
- 完成 Hono POST /api/sandbox/execute 端点
- 完成 CodeResultBlock "编辑并重新运行"按钮
- 完成 useCodeExec hook
- Tutor prompt 新增 executeCode 使用指引

**代码变更**：
- `docker-compose.yml` — 新增 judge0-server + redis-judge0 服务
- `.env.example` — 新增 JUDGE0_URL
- `apps/worker/src/sandbox/client.ts` — 新增 Judge0 REST API 客户端
- `apps/worker/src/sandbox/languages.ts` — 新增语言 ID 映射表（10 种语言）
- `apps/worker/src/agent/tools/execute-code.ts` — 新增 executeCode ToolDefinition
- `apps/worker/src/agent/tools/create-tools.ts` — 注册 executeCodeTool
- `apps/worker/src/agent/prompts/tutor.ts` — 新增 executeCode 指引
- `apps/server/src/routes/sandbox.ts` — 新增 POST /api/sandbox/execute
- `apps/server/src/services/sandbox.ts` — 新增 server 本地 sandbox client
- `apps/server/src/index.ts` — 挂载 sandbox route
- `apps/web/src/hooks/use-code-exec.ts` — 新增代码执行 hook
- `apps/web/src/components/ui-blocks/code-result-block.tsx` — 新增编辑并重新运行按钮
- `docs/设计/技术架构.md` — 更新 Docker/端口/项目结构/Agent 工具数
- `docs/设计/API接口.md` — 新增 sandbox/execute 接口文档
- `docs/设计/Prompt设计.md` — 新增 executeCode 工具定义
- `docs/开发/迭代计划.md` — 021 状态更新为已完成

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/server build` → 构建成功 ✅
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 24/24 E2E 测试全部通过 ✅
- LSP diagnostics 全部零错误 ✅

**下一步**：迭代 022（多 Agent 协作 + Subagent）

---

## 23:00 — 迭代 022：多 Agent 协作 + Subagent

**状态**：✅ 已完成

**事件**：
- 新增 `SubagentRegistry` 到 `@ai-teacher/agent` 公共包（`SubagentDefinition` 接口 + `AgentResult` 类型 + `SubagentRegistry` 类）
- 新增两个子 Agent 定义：`AssessmentAgent`（出题评估，3 步限制）+ `ResearchAgent`（资料检索，5 步限制）
- 新增 `delegateTask` 工具（工厂函数闭包模式捕获 SubagentRegistry + ToolRegistry）
- 子 Agent 使用 `glm-4-flash`（比主模型 `glm-5-turbo` 便宜），结果经 `toModelOutput()` 过滤防止上下文污染
- 工厂函数 `createTutorToolRegistry(subagentRegistry?)` 接收可选的 SubagentRegistry
- `TutorGraphContext` 新增可选 `subagentRegistry` 字段
- `chat-turn.ts` 创建 SubagentRegistry 并传入 graph context
- Tutor prompt 新增 delegateTask 使用说明

**技术决策**：
- 使用工厂函数闭包模式传递 SubagentRegistry，避免修改 `ToolExecutionContext` 核心类型
- 子 Agent 工具从主 ToolRegistry 中按名称筛选，复用已有工具定义
- `toModelOutput()` 每个子 Agent 自定义摘要策略（assessment ≤300 字，research ≤500 字）

**代码变更**：
- `packages/agent/src/types.ts` — 新增 SubagentDefinition, AgentResult 类型
- `packages/agent/src/subagent-registry.ts` — 新建 SubagentRegistry 类
- `packages/agent/src/index.ts` — 导出 SubagentRegistry + 新类型
- `apps/worker/src/agent/subagents/assessment.ts` — 新建 AssessmentAgent 定义
- `apps/worker/src/agent/subagents/research.ts` — 新建 ResearchAgent 定义
- `apps/worker/src/agent/subagents/index.ts` — 新建 createSubagentRegistry() 工厂
- `apps/worker/src/agent/tools/delegate-task.ts` — 新建 delegateTask 工具
- `apps/worker/src/agent/tools/create-tools.ts` — 工厂函数接收 SubagentRegistry
- `apps/worker/src/agent/tools/index.ts` — 新增 delegateTask 导出
- `apps/worker/src/graphs/tutor-graph.ts` — TutorGraphContext 新增 subagentRegistry
- `apps/worker/src/processors/chat-turn.ts` — 创建 SubagentRegistry 并传入
- `apps/worker/src/agent/prompts/tutor.ts` — 新增 delegateTask 使用说明

**验证结果**：
- `pnpm --filter @ai-teacher/worker build` → 新代码零错误 ✅（仅 pre-existing Prisma 类型问题）
- `pnpm --filter @ai-teacher/web build` → 构建成功 ✅
- 文档同步：技术架构.md（v0.8→v0.9）+ Prompt设计.md（v0.4→v0.5）+ 迭代计划.md ✅

**下一步**：迭代 023（技术栈全面升级：AI SDK v5 + Zod 4）
