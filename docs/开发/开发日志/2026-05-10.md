# 2026-05-10 开发日志

---

#### 2026-05-10 07:00 — 迭代 023 收尾：运行时验证 + 修复

**状态**：✅ 已完成

**事件**：
- 修复 Prisma `DATABASE_URL` 运行时错误：`apps/server/` 缺少 `.env` symlink（其他两个 app 已有）
- 修复 AI SDK v6 Provider 兼容性问题：默认使用 Responses API，智谱不支持，改为 `provider.chat()` 强制 Chat Completions API
- 修复 E2E 测试 `quick-question` content-type 断言：`text/plain` → `text/event-stream`
- 清除 `.next/` 缓存（依赖升级后 vendor chunks 过期）
- Docker Desktop → OrbStack 迁移完成，所有 5 个容器运行正常

**代码变更**：
- `apps/server/.env` — 新增 symlink → `../../.env`（与 web/worker 一致）
- `apps/worker/src/agent/provider.ts` — `getProvider()` 返回 `.chat.bind()` 而非默认 provider（避免 Responses API）
- `e2e/quick-features.spec.ts` — content-type 断言改为 `text/event-stream`
- `apps/web/.next/` — 删除缓存目录

**验证结果**：
- `pnpm build`（全量）web + worker + server 构建成功 ✅
- 所有 3 个服务启动正常：web :38421, server :38422, worker :38423 ✅
- `/api/sessions?userId=seed-user-ai-teacher` 返回 200 ✅
- `/api/suggested-topics` 返回 200 ✅
- E2E 测试 23/24 通过 ✅
  - 唯一失败：`suggest-reply` — 智谱 API 余额不足（`余额不足或无可用资源包,请充值`），非代码问题

**下一步**：充值智谱 API 后重新运行 E2E 确认 24/24 通过；或开始新功能迭代

---

#### 2026-05-10 22:30 — E2E Mock 策略：24/24 测试 11 秒通过

**状态**：✅ 已完成

**事件**：
- 解决智谱 API 余额不足问题：改用 MOCK_LLM=true 服务端 mock，不再依赖真实 LLM API
- Provider 级 mock：`MockLanguageModelV3`（`ai/test`）模拟 `doGenerate` + `doStream`
- Route 级 mock：`sessions.ts`（buildFallbackNodes）、`suggest-reply.ts`（静态 JSON）、`quick-question.ts`（raw SSE Response）
- 模型统一：所有 `glm-4-flash` → `glm-5-turbo`（assessment/research subagent + delegate-task 默认值）
- 新增 `e2e/fixtures/db.ts`（DB cleanup fixture）
- Playwright 配置：`MOCK_LLM=true` 注入到 server/worker webServer 命令，timeout 30000

**代码变更**：
- `apps/worker/src/agent/provider.ts` — MockLanguageModelV3 mock provider + base URL 修正
- `apps/worker/src/agent/subagents/assessment.ts` — glm-4-flash → glm-5-turbo
- `apps/worker/src/agent/subagents/research.ts` — glm-4-flash → glm-5-turbo
- `apps/worker/src/agent/tools/delegate-task.ts` — glm-4-flash → glm-5-turbo
- `apps/server/src/routes/sessions.ts` — MOCK_LLM quick path（buildFallbackNodes）
- `apps/server/src/routes/suggest-reply.ts` — MOCK_LLM quick path（静态 JSON）
- `apps/server/src/routes/quick-question.ts` — MOCK_LLM quick path（raw SSE Response）
- `e2e/fixtures/db.ts` — 新增 DB cleanup fixture
- `e2e/home.spec.ts` — timeout 调整 120000→60000
- `playwright.config.ts` — MOCK_LLM=true + timeout 60000→30000

**验证结果**：
- `pnpm build` 通过 ✅
- E2E 24/24 通过（11 秒）✅
- 4 commits pushed to origin/main ✅
  - `fix: 默认模型 glm-4-flash → glm-5-turbo — subagent + delegate-task`
  - `feat: MOCK_LLM mock provider — MockLanguageModelV3 + base URL 修正`
  - `feat: MOCK_LLM route 级 mock — sessions + suggest-reply + quick-question`
  - `test: E2E mock 基础设施 — Playwright MOCK_LLM + DB cleanup + timeout 调整`

**下一步**：迭代 024（待定）或产品优化

---

#### 2026-05-10 21:10 — 迭代 027-029 规划

**状态**：✅ 已完成

**事件**：
- 规划下一阶段 3 个迭代，按技术维度拆分
- 迭代 027（聊天内诊断重构）：诊断从独立 REST API 改为聊天内 askQuestion 工具 + 选择题 UI + hidden 消息
- 迭代 028（SSE 事件扩展）：新增 roadmap-updated / session-updated 实时推送事件
- 迭代 029（掌握报告与自动过渡）：掌握后渲染总结报告（表格+徽章），Agent 自动过渡到下一节点

**代码变更**：
- `docs/开发/迭代详情/027-聊天内诊断重构.md` — **新建**
- `docs/开发/迭代详情/028-SSE事件扩展.md` — **新建**
- `docs/开发/迭代详情/029-掌握报告与自动过渡.md` — **新建**
- `docs/开发/迭代计划.md` — 更新迭代总览表 + 详情索引 + 版本号 v1.2

**验证结果**：
- 文档已完成，待用户确认后开始实施

**下一步**：迭代 027 实施（等待用户确认）
