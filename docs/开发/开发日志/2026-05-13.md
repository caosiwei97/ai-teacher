# 开发日志 — 2026-05-13

## 迭代 037：代码编辑器优化 + 沙箱增强

### Bug 修复

1. **右侧面板折叠后消失**
   - 根因：展开按钮的响应式类 `lg:hidden` 导致桌面端隐藏
   - 修复：合并为单个按钮，统一 `hidden lg:block`
   - 文件：`apps/web/src/app/(app)/learn/[sessionId]/page.tsx`

2. **编辑器清空后高度塌缩**
   - 根因：CodeMirror 容器空内容时高度为 0
   - 修复：容器加 `min-h-[200px]`
   - 文件：`apps/web/src/components/editor/code-editor.tsx`

### UI 优化

3. **运行按钮移到工具栏**
   - 从底部全宽大按钮 → 工具栏右侧 Play icon 小按钮
   - 新增 Copy 按钮（点击后 ✓ 1.5s 恢复）
   - 清空按钮改为纯图标，风格统一
   - 文件：`apps/web/src/components/sidebar/code-panel.tsx`

4. **编辑器增强**
   - 启用行号栏（`lineNumbers()`）
   - 当前行高亮（`highlightActiveLineGutter()` + `highlightActiveLine()`）
   - 语法高亮（`syntaxHighlighting(defaultHighlightStyle)`）
   - 括号匹配（`bracketMatching()`）
   - 文件：`apps/web/src/components/editor/code-editor.tsx`

5. **输出区域加标题**
   - 新增 "Output" 小标题 + 执行耗时显示
   - 文件：`apps/web/src/components/sidebar/code-panel.tsx`

### 沙箱增强

6. **API Key 注入**
   - 创建沙箱时注入 `OPENAI_API_KEY` + `OPENAI_BASE_URL`
   - 带安全警告注释（TODO: Web 服务时改 API 代理）
   - 文件：`apps/server/src/services/sandbox.ts`

7. **Agent Prompt 更新**
   - executeCode：沙箱有 key 可直接调用 openai，缺库先 pip install
   - tutor 核心规则第 7 条同步更新
   - 文件：`apps/worker/src/agent/tools/execute-code.ts`、`apps/worker/src/agent/prompts/tutor.ts`

### 诊断增强

8. **编程主题语言偏好题**
   - 方案 A：仅改 prompt，不改前端和数据模型
   - 编程主题诊断自动加一道语言偏好题（Python/JS/TS/Java/C++/其他）
   - 文件：`apps/worker/src/agent/tools/ask-question.ts`

### 开发体验

9. **dev 命令优化**
   - `pnpm dev`：保留 `.next` 缓存，秒启动
   - `pnpm dev:fresh`：清 `.next` 后启动（改 CSS 变量/env/配置时用）
   - 文件：`package.json`、`apps/web/package.json`

---

## 文档结构重整

#### 2026-05-13 20:00 — 文档结构审查与重整

**状态**：✅ 已完成

**事件**：
- 审查文档结构，发现冗余、缺失和过时问题
- E2E 测试策略调整：日常 commit 只需 build 通过，发版时跑完整 E2E

**文档变更**：

归档：
- `docs/设计/架构升级方案.md` — 标记为已实施（迭代 016-022）
- `docs/设计/Agent引擎优化方案.md` — 标记为已实施（迭代 011-012）

精简：
- 删除 `docs/测试/e2e/01~12-*.md` 12 个详情文件，只保留 README 矩阵总览
- 删除 `docs/design/` 目录，移 `layout-comparison.excalidraw` 到 `docs/设计/diagrams/`
- 重命名 `设计系统v2.md` → `设计系统.md`，修复所有引用（4 处）

新增：
- `docs/设计/错误降级策略.md` — 错误处理分层、重试/降级策略、超时控制、已知缺陷
- `docs/设计/安全设计.md` — 沙箱安全、密钥管理、威胁模型、已知风险
- `docs/测试/发版测试清单.md` — 发版前测试流程、E2E 矩阵、手动验证项

更新：
- `AGENTS.md` §3.4 质量门控 — commit 前只需 build，E2E 改为发版时运行
- `docs/README.md` — 索引重写，反映新文档结构
- `docs/测试/e2e/README.md` — 更新版本号、去掉已删除文件的链接

**验证结果**：
- 所有文档链接检查：引用 `设计系统v2.md` 的 4 处已全部更新 ✅
- `docs/design/` 目录已清理 ✅

**下一步**：迭代 009（学习资料上传 + RAG）或迭代 038（沙箱多文件工程结构）或产品优化
