# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 1（迭代 050）✅ **人工验收通过**。①删掌握仪式 + ②HTML 沙箱渲染器 + ③形态 A 互动课产出（few-shot 加持 agent 主动调 renderUI）+ interactive 推右栏（像代码沙箱）。dev 真实 LLM 走查：删仪式≤1句、互动课三段式可交互、对话轮次收敛。详见迭代详情 050。
- **全门控**：typecheck ✅ / test:unit 125 ✅ / check:docs ✅ / lint 0 error / check:deps ✅ / web build ✅ / **E2E 58/58 ✅**。
- **验收修复**：③prompt 加 few-shot HTML 骨架（agent 主动调 renderUI interactive，非只文字说）；interactive 推右栏（RightSidebar 互动课 Tab + learn.tsx 检测推右栏 + index.tsx 过滤不内嵌）；mastery.spec 容错点 Tab。dev 环境修复：prisma migrate（source 列）+ API 配 deepseek default LlmConfig。
- **遗留**：互动课 HTML 偶有脚本 bug（LLM 生成质量，非机制）；独立 session chat-turn 未跑通（interactive-lesson 暂用 seed session）。
- **下一步**：050 ✅ 已完成。可进入 051 复习模式 / 052 面试模式 / 053 UI 统一工作台。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
