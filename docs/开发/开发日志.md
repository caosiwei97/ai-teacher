# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：产品体验 7 问题分 3 组推进，已完成**第 1 组（两个 Bug 修复）**。①result 接口崩溃（`prisma.interviewResult` undefined）三重防御——InterviewService 查询型返回 null/写入型抛错（startOrGet 统一抛错与写入型一致 + getResult 声明联合类型）、server/worker 启动校验 fail-fast、dev/fresh/start 脚本前置 `db:generate`。②诊断路线图未渲染——`learn.tsx` 校验 generateRoadmap `success`+nodes 非空才置标志，失败显示错误+可重试（diagnosticFailed 标志 break 不 return，正常走 fetchSession），fetchSession 兜底延迟重试。
- **文档同步**：API 文档已记录 `result: null` 语义（delegate 缺失容错同语义，无需改）；check:docs 全绿。待同步文档变更为空。
- **验证**：`pnpm test:unit`（186）、`pnpm lint`（0 errors）、`pnpm check:deps`、`pnpm check:docs`、`pnpm --filter @ai-teacher/web build` 全绿。E2E 新增 diagnostic 一致性断言 + interview result 200 断言（待服务栈启动后统一跑）。
- **下一步**：进入**第 2 组 Agent Loop 健壮性迭代**（SSE 协议统一 + abort 通道 + failover 分层降级 + 循环检测注入提示+熔断 + 回答中禁用切换），plan 已写在 `docs/superpowers/plans/2026-06-30-agent-loop健壮性迭代.md`。
- **注意**：Task4 审查遗留 2 个 Minor（diagnosticFailed break 路径 reader 未 cancel 释放；diagnosticError 的 setTimeout 重试竞态），不阻塞，可作 follow-up。分支 `feat/agent-loop-experience-and-robustness`。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
