# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 1（迭代 050）子任务②**HTML 沙箱渲染器 ✅ 完成**。①删掌握仪式 + ②interactive block（schema + iframe 沙箱 + 净化 + 降级 + 既有债梳理）已完成（①已推送 main commit 20b5194，②待提交）。详见迭代详情 050。
- **全门控**：typecheck ✅ / test:unit 125 ✅ / check:docs ✅ / lint 0 error / check:deps ✅ / web build ✅ / **E2E 58/58 ✅（含分类 Q interactive-lesson）**。
- **②顺手修预存**：chat-turn mock 模式解密错误（getProviderForJob 短路 MOCK_LLM，消除 seed key 解密失败 job）；mock doStream 加 [render-interactive] 分支支持 E2E tool-call。①环境备注（pnpm install 补装/prisma generate）同前。
- **下一步**：提交② → 子任务③形态 A 互动课落地（教练式 prompt 加互动课产出指引三段式 + 规则1铺垫放宽 + 调用时机收敛 1-2 轮）。③后统一人工验收（删仪式啰嗦度 + 互动课易懂性，spec §9.3）。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
