# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 2（迭代 051 复习模式）✅ **已完成**（2026-06-29）。①②③ 全完成：①数据层+间隔重复算法 TDD；②复习 Agent+API（考官 prompt + recordReviewResult tool + chat-turn activeMode 分流 + ReviewService + review API + flashcard block）；③复习 UI（FlashcardBlockRenderer 翻面/自评 + 右栏复习清单 + 开始复习切模式）+ review.spec.ts（分类 R，3 用例）。修复 isReviewDue 兼容 BullMQ 序列化的 ISO 字符串。全门控绿 / **E2E 61/61**（58+3）。dev 真实 LLM 走查：考官产 flashcard block（结构匹配 renderer）。详见迭代详情 051。
- **下一步**：进入 052 面试模式文本版（Phase 3，spec §4）。面试官 prompt（设定→拷问→即时评分→复盘，全程不讲解只追问）+ 评分 tool + 三档难度动态调整 + 复盘报告。新增 interview.spec.ts（分类 S）。详见 spec §4/§9.2 Phase 3。
- **注意**：052 复用 051 的 activeMode 分流模式（interview 分支）+ 评分持久化（可仿 ReviewService）；面试模式范围=已掌握知识点（同复习渐进解锁）。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
