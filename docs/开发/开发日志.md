# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 3（迭代 052 面试模式文本版）🔧 进行中。①② ✅ 完成。①数据层+面试评分算法 TDD；②面试 Agent+API：面试官 prompt（interview.ts，全程不讲解只追问+三档难度+高压）+ scoreAnswer/finalizeInterview tool + chat-turn interview 分流（加载 InterviewResult 注入难度/streak/题数）+ InterviewService（startOrGet/scoreAnswer/finalize/getResult）+ interview API（GET /result）+ interviewScore block。dev 真实 LLM 走查通过（面试官风格+出题追问+无讲解性陈述）。全门控绿 / E2E 61/61。详见迭代详情 052。
- **下一步**：052 子任务③ 面试 UI + E2E。面试入口（右栏 面试 tab，红色基调）+ InterviewScoreBlockRenderer（评分卡/复盘）+ interview.spec.ts（分类 S：不讲解断言 + 难度调整 + 评分卡/复盘）。详见 spec §4.1/§5.1 + 052 详情③。
- **注意**：③ 需加 InterviewScoreBlockRenderer（registry）；interview.spec.ts mock LLM 走查面试流程（mock 加 [score-answer]/[finalize-interview] 标记）；finalizeInterview 端到端在 ③ E2E 完整覆盖。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
