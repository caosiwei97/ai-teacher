# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 3（迭代 052 面试模式文本版）✅ **已完成**（2026-06-29）。①②③ 全完成：①数据层+面试评分算法 TDD（adjustDifficulty 连续2答对升/答错降 + computeTotalScore + InterviewResult model）；②面试 Agent+API（面试官 prompt 全程不讲解只追问 + scoreAnswer/finalizeInterview tool + chat-turn interview 分流 + InterviewService + interview API + interviewScore block）；③面试 UI（InterviewScoreBlockRenderer 评分卡 + 右栏面试 tab 红色基调 + 开始面试）+ interview.spec.ts（分类 S，2 用例）。顺手修 global-setup/teardown resetTestDb 删除顺序（session 须在 source/llmConfig 前删，修预存外键报错）。全门控绿 / **E2E 63/63**（61+2）。dev 真实 LLM 走查：面试官风格+出题追问+无讲解性陈述。详见迭代详情 052。
- **下一步**：进入 053 UI 统一工作台 + 响应式（Phase 4，spec §5）。顶部 Tab（学/固/验切换是状态变化非页面跳转）+ 渐进解锁（0 知识点灰锁）+ 落地页 + 起步页 + 产物聚焦面板（超宽屏）。全站 UI 重构，E2E 影响最大（learn/session-management/quick-features 全面重写）。详见 spec §5/§9.2 Phase 4。
- **注意**：053 是 2.0.0 收官，E2E 影响面最大，建议单独评估是否拆 E2E 专项；三模式 Tab 切换 + 渐进解锁复用 051/052 的 activeMode；语音（2.1.0/054）依赖 049 DashScope KEY。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
