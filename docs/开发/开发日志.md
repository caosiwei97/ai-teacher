# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 3（迭代 052 面试模式文本版）🔧 进行中。子任务① ✅ 完成：数据层 + 面试评分算法 TDD。`packages/shared/src/services/interview-scoring.ts` 纯函数（adjustDifficulty 连续2答对升/答错降 + computeTotalScore，TDD 12 单测）+ Prisma（InterviewStatus/InterviewDifficulty enum + InterviewResult model）+ migration `20260629061112_add_interview`（HNSW drift 处理）。全门控绿 / E2E 61/61。详见迭代详情 052。
- **下一步**：052 子任务② 面试 Agent + API。面试官 prompt（interview.ts，设定→拷问→即时评分→复盘，全程不讲解只追问，三档难度，高压）+ scoreAnswer tool（每题评分→adjustDifficulty+更新 InterviewResult）+ finalizeInterview tool（复盘→computeTotalScore+薄弱点+改进建议）+ chat-turn interview 分流 + InterviewService + 面试 API（GET /interview/result 评分卡）+ interviewScore block。详见 spec §4.1/§4.3 + 052 详情②。
- **注意**：难度状态跨轮持久化（InterviewResult.difficulty/streak），chat-turn 每轮加载注入 prompt；"全程不讲解"prompt 指令 + E2E mock 断言。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
