# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 4（迭代 053 UI 统一工作台）①②③ ✅ **已完成**（2026-06-29，053 收官 + 2.0.0 收官）。③右栏折叠浮标（🗺 路线 mastered/total 摘要 + 唤回）+ E2E 重写（home/session-management/review/interview 适配 ①② 顶部Tab/落地页/左栏 + 新增 `mode-switch.spec.ts` 分类 T 6 用例：Tab切换URL不变+渐进解锁两态+响应式三断点）+ E2E 矩阵同步（迭代计划/README/质量门控 加 T）。**产物聚焦面板降级**（决策4，保留内嵌+右栏 tab，记后续增强）。E2E 污染修复：① 顶部 Tab PATCH DB activeMode 污染共享会话 → `workers:1` 串行 + review 末尾恢复 + learn.tsx `setActiveMode(session.activeMode)` 初始化 + `selectSession` 改 `navigate`（修落地页点会话不跳转）。全门控绿 / **E2E 69/69**。③ 后 fix（人工走查发现）：诊断提交后立即"已提交"（不滞后 agent 分析）+ 互动课从右栏 320px 改内嵌对话气泡 max-w-3xl（移除 MessageContent interactive 过滤 + right-sidebar interactive tab）+ 互动课 iframe 高度自适应（sandbox 无 same-origin，改 postMessage 内容高度，选项不挤，实测 240→706px）+ 注入 CSS 覆盖 agent HTML 紧凑布局（div[style*=flex] block + button 垂直全宽宽松间距，实测 735→943px）+ 改选择器 div[style*=flex-wrap]>\* 匹配 agent 畸形 buttona（<buttona")'>，offsetHeight 735→983px。
- **下一步**：053 人工走查（spec §9.3 视觉吸引力 + 三模式切换流畅度，需真人）。2.0.0 收官，后续 2.1.0 面试语音（054，依赖 049 DashScope KEY）。遗留：产物聚焦面板增强 + 起步页完整替代诊断仪式（改 agent）+ `tutor.ts:31` interviewer 死代码清理 + E2E README 用例数合计过时（既有债，实际 69）。
- **注意**：053 产物聚焦面板降级（spec §5.4/§9.2 偏离，决策4 已确认）；playwright `workers:1`（串行避免 ① 顶部 Tab PATCH 污染共享会话，发版 E2E 30s 可接受）；语音 054 依赖 2.1.0。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
