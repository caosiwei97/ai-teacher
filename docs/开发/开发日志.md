# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：1.2.0 学习资料 + RAG（迭代 009 Phase 1）✅ **全部完成并验证**。M1（数据层+存储）/ M2（解析+智谱 embedding-3+pgvector 检索+sources 路由+source-processing worker）/ M3（retrieve-context 工具 + 上传 UI + E2E 分类 K）三里程碑全绿。全门控通过：typecheck / lint(0 error) / check:deps / test:unit(93) / check:docs / web build / **E2E 54/54**。
- **下一步**：009 Phase 2（视频理解）V1 ✅ **完成并真实验证**。架构经 spike 实证定型：视频字节 → 上传 DashScope 文件 API（getPolicy→OSS）→ `oss://` 临时 URL → Qwen3.5-Omni（带 `X-DashScope-OssResourceResolve` 头）→ 教学向 digest → 复用 Phase 1 RAG。38MB YouTube 18min 视频实测：准确提取口语讲解（点名讲者、引用原话）+ 时间戳分节，**音视频理解确认**。全门控绿（typecheck/lint0/deps/test:unit110/check:docs/build）。**base64 + MinIO presigned 方案均弃用**（前者 413 体积上限，后者本地不可达），`getPresignedUrl`+`@aws-sdk/s3-request-presigner` 已删。下一步 **V2**：worker video 分支（download/upload→understand→chunk→embed→store）+ sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。**B站 链接需 SESSDATA**（反爬 412；macOS 浏览器 cookie 不可靠）→ V2 在设置页加 SESSDATA 字段（加密存，复用 LLM key 那套）。
- **注意**：`youtube-dl-exec` 二进制已就位（pnpm onlyBuiltDependencies 已 approve）。方案见 `迭代详情/0.1.0/009-资料上传RAG.md` Phase 2 节 + ADR-029。
- **已知遗留**：E2E worker 的 Prisma 连接在本机环境系统性报 "Server has closed the connection"（chat-turn job 同样受影响，现有 E2E 因宽松断言通过）。我的 sources E2E 据此采用宽松断言（上传→列表→删除）。根因待查（疑似 tsx watch + 测试库连接生命周期），非 009 代码 bug。
- **顺手修复**：① eslint 漏忽略 `.next` 本地构建产物（本地 lint 误报 381 error，CI 无此问题）；② E2E 与 dev 共享 redis 致 worker 串台 → E2E 改用 redis DB 1 隔离（playwright.config）。
