# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 0（模型配置引导，迭代 049）✅ **代码完成并已并入 main**（线性提交，`feat/phase0-model-config` 分支与 main 指向同一 commit，无需再合并；20 个 commit 尚未推送至 `origin/main`）。各层改动：
  - DB：`LlmConfig` 加 `source` 字段（user/env 标记来源）
  - shared：`ConfigSource` 枚举 + `backfillLlmConfigsFromEnv`（env→DB 回填，幂等，5 单测）+ `resolveProviderConfig`（provider 选择补"查用户 default 配置"，修复既有缺口，3 单测）
  - server：启动钩子挂回填（fire-and-forget）+ API 响应带 source + env-status 扩展（hasDefaultDbConfig/dbConfigCount）
  - web：LlmConfig 类型加 source + settings 按功能分组（对话模型/增强能力）+ 来源标记（"来自 .env"）+ home 页主模型未就绪软引导（可跳过进入）
  - 文档：.env.example 回填说明 + 技术架构 LlmConfig schema 同步
- **全门控**：typecheck ✅ / shared 单测 ✅（回填5+provider3）/ web build ✅ / **E2E 57/57 ✅**（含 Phase 0 的 source/env-status 断言）。
- **✅ 迁移顺序 bug 已修复**（commit cd3b1d3）：迭代009 的 `20260626150411_add_video_source` 时间戳早于 enum 迁移，已改名 `20260626170000` + 同步 dev 库 `_prisma_migrations` 历史。
- **✅ E2E 时序竞态已修复**（commit 6977168）：根因是 prisma 全局单例（零重连）+ global-setup `DROP DATABASE` 击穿持续运行的 dev server 连接池（main 上也复现）。修法：global-setup 改幂等建库（不再 drop）+ reset 清数据（补 documentChunk）+ assertNoDrift 护栏（防 schema 漂移复发）。**全量 E2E 57/57 通过（19.6s），Phase 0 E2E 全绿。**
- **未做（计划内标注的范围限制）**：增强能力 KEY（Embedding/DashScope）的完整 DB 化（新建 ServiceConfig model）留后续子任务，本 Phase 0 只做 settings 分组占位。
- **下一步**：① 推送 main → `origin/main`（Phase 0 的 20 个 commit 已在本地 main，未推送）；② 进入 Phase 1（互动产物能力 + 学习模式改造，迭代 050）。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。**⚠️ V2 的迁移文件 `20260626150411_add_video_source` 时间戳写早了**（应晚于 enum 迁移），见上面 E2E 阻塞。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
