# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：互动练习从「LLM 手写整段 HTML + iframe 沙箱渲染」重构为「结构化 A2UI（三段式 JSON：title/concept/explore[slider|input]/quiz，前端 React 渲染）」。解决三个问题：①慢——LLM 只生成结构化数据，token 大减、流式提前可见；②图片撑破布局——前端 React 可控布局 + 补 `min-h-0`；③提交无响应——消除 iframe postMessage 竞态 + 去掉 `chat.isLoading` 静默吞提交。删除 `sanitize-html.ts` 净化模块。`[Interactive Response]` 续接协议不变。
- **文档同步**：`Prompt设计.md`（互动课产出段 + renderUI 工具定义）、`技术架构.md`（删 sanitize-html.ts 引用 + interactive renderer 描述）、`决策记录.md`（ADR-030）、`050 迭代详情`（加设计演进注释）均已同步；待同步文档变更为空。
- **验证**：`pnpm -r typecheck`（5 包）、`pnpm test:unit`（182）、`pnpm lint`（0 errors）、`pnpm check:deps`、`pnpm check:docs`、`pnpm --filter @ai-teacher/web build` 全绿；`pnpm test:e2e e2e/interactive-lesson.spec.ts e2e/learn.spec.ts`（17 passed）已通过。
- **下一步**：真人走查 Chrome 新建"个人投资理财入门"流程，重点看真实 LLM 生成结构化互动课的等待提示、slider/选项交互、自测提交续接；继续后续候选：产物聚焦面板增强、会话批量删除、标题手动重命名、2.1.0 面试语音（054）。
- **注意**：E2E 仍会打印既有 `[backfill] 回填失败（不阻塞）` Prisma FK warning，属既有测试环境噪声。`explore` 只支持 slider/input 两种交互，牺牲了自由 HTML 任意交互（代码示例走独立沙箱 pushCode）。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
