# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：已修复路线生成后第一课互动产物等待态缺失、互动卡提交后流程不续接、互动卡视觉与诊断卡割裂的问题。互动卡现在使用诊断卡同款 header/footer，提供明确“完成自测”提交按钮，提交后通过隐藏 `[Interactive Response]` 自动续接 Tutor；路线生成后续接第一课期间显示“路线已生成，正在准备第一节互动练习…”而非泛化“老师正在思考中…”。
- **文档同步**：已同步 `docs/设计/Prompt设计.md` 的 `[Interactive Response]` 隐藏消息协议；`docs/开发/迭代计划.md` 状态不变（2.0.0 已完成，本次为 bugfix）。
- **验证**：`pnpm -r typecheck`、`pnpm test:unit`、`pnpm lint`（0 errors，既有 warnings）、`pnpm check:deps`、`pnpm check:docs`、`pnpm --filter @ai-teacher/web build`、`pnpm test:e2e e2e/interactive-lesson.spec.ts e2e/learn.spec.ts`（17 passed）已通过；补充等待态断言后 `pnpm test:e2e e2e/learn.spec.ts`（16 passed）已通过。
- **下一步**：真人走查 Chrome 新建“个人投资理财入门”流程，重点看真实 LLM 生成互动课时的等待提示、自测按钮提交、是否自动进入下一轮；继续后续候选：产物聚焦面板增强、会话批量删除、标题手动重命名、2.1.0 面试语音（054）。
- **注意**：E2E 过程中仍会打印既有 `[backfill] 回填失败（不阻塞）` Prisma FK warning，部分 sandbox ECONNRESET 也属既有测试环境噪声；待同步文档变更为空。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
