# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 042 完成（计算性传感器：ESLint no-default-export 自定义规则 + 自定义依赖方向检查 check-deps.mjs + Prisma 7 个状态字段 Enum 迁移含 Node.status 改名 not_started/in_progress；吸收 046 Phase 2；新增 048 跟踪 5 处 server↔worker 依赖技术债）
- **下一步**：046 Phase 1（5 个索引）/ 048（server/worker 共享代码归位，先决策 provider-registry/profile-service/diagnostic 归属）/ 047（Redis/MinIO 闭环）
- **注意**：042 发现的 5 处依赖违规已白名单豁免（ADR-024），check-deps.mjs 的 KNOWN_VIOLATIONS 待 048 清空；CI 已加 lint + check:deps 步骤；E2E 未跑（Node.status 值已在 E2E 同步改名，发版前需回归）
