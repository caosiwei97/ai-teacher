# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 2（迭代 051 复习模式）🔧 进行中。子任务① ✅ 完成：数据层 + 间隔重复算法 TDD。`packages/shared/src/services/spaced-repetition.ts` 纯函数（isReviewDue/computeNextInterval/applyReviewResult，TDD 14 单测）+ Prisma（ActiveMode enum + Session.activeMode + Node 四字段 memoryStrength/lastReviewedAt/nextReviewAt/reviewInterval）+ migration `20260629040841_add_review_mode`（沿用既有约定手动移除 HNSW 索引 DROP + 重建+校准 checksum）。全门控绿 / E2E 58/58。详见迭代详情 051。
- **下一步**：051 子任务② 复习 Agent + API。考官 prompt（提取练习不重讲，答错才提示）+ 抽认卡/回忆测验/错题本 tool + chat-turn 按 session.activeMode 分流（review→考官 prompt+复习 tools）+ 复习 API（今日到期清单 GET + 提交结果 POST 更新记忆强度，调用 ① 的 applyReviewResult）。详见 spec §3.2/§3.3 + 051 详情②。
- **注意**：chat-turn 分流需兼顾既有学习模式不受影响；review.spec.ts 在③写。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
