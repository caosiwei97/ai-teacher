# AI Teacher — 开发日志

> 记录开发过程中的关键时间节点和决策，防止上下文溢出后丢失信息。
> 仅保留近 3 天的日志。条目格式：事件概述（1-2 句）+ 关键决策（仅 WHY）+ 提交引用。代码变更详情见 `git log`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。格式见 AGENTS.md §3.5 -->
<!-- 下一个 session 启动时消费：更新文档 → 移除条目 → 记入当日日志 -->

---

## 里程碑时间线

<!-- 仅保留近 3 天的里程碑。 -->

---

## 日志详情按日期归档

| 日期 | 主要内容 |
|------|----------|
| （暂无近 3 天日志） | — |

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 001-040 全部完成，1.0.0 版本进行中（文档已全面同步）
- **下一步**：迭代 041（Agent 引擎重构 — 删壳归位）或 迭代 009（学习资料上传 + RAG）
- **注意**：Docker 已迁移到 OrbStack；`apps/web/.env` symlink 需保留；provider 使用 `@ai-sdk/deepseek` 原生 provider
- **前端框架**：Vite 8 + React 19 SPA + react-router v7，路由在 `src/router.tsx`，页面在 `src/pages/`，布局在 `src/layouts/`
- **编辑器**：Monaco Editor（Catppuccin 暖色主题），IDE 布局（文件树 + Monaco + xterm.js 交互终端），通过 SandboxProvider 管理状态
- **沙箱**：OpenSandbox（Judge0 API 兼容），Hono Server 代理文件系统 + PTY WebSocket
- **左侧栏**：折叠态为图标栏（Logo/新建/消息/主题/设置/展开）
- **主题系统**：深色/浅色/跟随系统三种模式，ThemeProvider + ThemeToggle 在左侧栏
- **多模型**：后端 + 前端均已完成，设置页 `/settings`
- **组件流式生成**：renderUI blocks 通过 `StreamingBlockParser` 逐个流式渲染
- **dev 命令**：`pnpm dev` 秒启动（Vite ~200ms），`pnpm fresh` 清 dist 重启
- **环境变量**：客户端变量用 `VITE_` 前缀（如 `VITE_API_URL`），服务端用 `process.env`
