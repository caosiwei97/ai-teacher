# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：1.1.0 工程化治理全部完成（042-048）并**发版验证通过**。E2E 全量回归 53/53 绿。修复了 E2E 测试基础设施 bug：`global-setup.ts` 改为每次 drop+recreate 测试库再 migrate deploy，杜绝迁移历史与 schema drift（原"仅当不存在才创建"导致测试库 schema 过时时新迁移静默跳过，042 enum 迁移首次暴露此问题）。
- **下一步**：1.2.0 学习资料 + RAG（迭代 009，P3）— 启动时一并实施 047 Phase 2（MinIO 存储）+ Phase 3（视评估）
- **注意**：1.1.0 已发版（042-048 + E2E 修复均已提交推送）；shared 现含 ai SDK 依赖但经子路径隔离未进 web bundle（已验证）；047 Phase 2/3 暂缓待 009
