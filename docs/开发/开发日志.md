# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：✅ 课程推进 P0 修复完成。诊断答案确定性生成路线图；同会话 active turn 409 防重入；`[Continue]` 自动续接只暴露教学产出工具，互动提交才允许 `assessMastery`；interactive 绑定 nodeId，历史卡刷新后锁定；缺 interactive 时补兜底自测；最终节点掌握后 session 自动 completed。
- **文档同步**：API接口.md 已补 `/api/chat` 409、自动续接权限与兜底；Prompt设计.md 已补工具边界和 nodeId 绑定；决策记录.md 新增 ADR-033。
- **验证**：真实 `/api/chat` 路径完整完成「个人投资理财入门」10/10，页面完成区可见 `个人投资理财入门 ✓`；900px 小屏左栏自动折叠；typecheck、unit、E2E、check:docs、check:deps、web build 通过。
- **下一步**：提交并推送本轮修复；如继续优化，优先处理 E2E 日志里的既有 backfill 外键 warning 与 sandbox ECONNRESET。
- **注意**：自动续接兜底卡是保障课程不断流的通用卡，不替代模型生成的优质互动课；`[Continue]` 轮不应拥有评估权限。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
