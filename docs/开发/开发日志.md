# AI Teacher — 开发日志

> 记录开发过程中的关键时间节点和决策，防止上下文溢出后丢失信息。
> 仅保留近 3 天的日志。条目格式：事件概述（1-2 句）+ 关键决策（仅 WHY）+ 提交引用。代码变更详情见 `git log`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。格式见 AGENTS.md §3.5 -->
<!-- 下一个 session 启动时消费：更新文档 → 移除条目 → 记入当日日志 -->

---

## 里程碑时间线

<!-- 仅保留近 3 天的里程碑。 -->

- **2026-05-23**：全量文档审计，修复 6 个文件的 15+ 处文档与代码不一致

---

## 日志详情按日期归档

| 日期 | 主要内容 |
|------|----------|
| 2026-05-23 | 全量文档审计：技术架构/API 接口/决策记录/E2E/README |

### 2026-05-23 全量文档审计

**背景**：用户要求检查代码库实现与当前文档的差异，发现文档多处滞后于代码演进。

**审计方法**：并行 3 个 explore Agent（项目结构 + API 路由 + 技术栈）+ 直接文件审查。

**发现并修复的问题**（按严重度排序）：

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | 技术架构.md | `packages/agent/` 幽灵引用（已删除但仍出现在架构图）+ StateGraph/ToolRegistry 重复架构描述（~80行） | 删除幽灵架构图和重复的 §5.1-5.2 |
| 2 | 决策记录.md | ADR-001 仍写"智谱 GLM"（实际已用 DeepSeek） | 重写为 DeepSeek + 多 Provider |
| 3 | 决策记录.md | ADR-003 缺少 Server 端口 38422 | 补全端口表 |
| 4 | 决策记录.md | ADR-005 写"v4"、ADR-019 写"v5"（实际 v6） | 更新为 v6 |
| 5 | 决策记录.md | ADR-008 描述"Web 直接调用 Worker"（已废弃） | 标记为已废弃，指向 ADR-013/020 |
| 6 | API接口.md | `GET /api/llm/env-status` 端点未文档化 | 新增 §14.7 |
| 7 | API接口.md | `POST /api/sessions` 缺少 teachingMode/llmConfigId 参数、初始状态错误（写 diagnosing 实际 active） | 修正请求体和响应示例 |
| 8 | API接口.md | `POST /api/chat` 未文档化自动创建会话行为 | 新增说明段落 |
| 9 | API接口.md | `GET /api/suggested-topics` 示例数据过时 | 更新为实际硬编码数据 |
| 10 | API接口.md | `GET /api/llm/models` 响应缺少 provider/name 字段 | 修正响应示例 |
| 11 | API接口.md | `POST /api/sandbox/execute` 缺少 llmConfigId 参数 | 补充参数 |
| 12 | e2e/README.md | 4 个 E2E spec 文件未在测试矩阵中（sandbox-ide/suggested-topics/session-detail/llm-config） | 新增分类 M/P/O/P |
| 13 | README.md | `docs/产品/需求规格.md` 链接指向不存在的文件 | 删除幽灵链接 |
| 14 | README.md | 技术栈表仅写 DeepSeek（实际支持 9 Provider） | 更新为多 Provider 描述 |
| 15 | README.md | `data/` 目录缺少 `redis-judge0/` | 补充说明（历史遗留） |
| 16 | 迭代计划.md | E2E 映射表缺少 4 个独立测试文件说明 | 添加注释 |

**未修改（已知但保留）**：
- ADR-017（CodeMirror→Monaco）和 ADR-018（Judge0→OpenSandbox）保留原记录，因为 ADR 是历史决策文档，ADR-021 已记录 Monaco 替换决策
- `sigma/` 目录未在文档中提及（项目辅助目录，非核心）
- `VITE_API_URL` vs `API_SERVER_URL` 命名差异需进一步验证

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 001-040 全部完成，1.0.0 版本进行中（文档已全面同步）
- **下一步**：迭代 041（Agent 引擎重构 — 删壳归位）或 迭代 009（学习资料上传 + RAG）
- **注意**：Docker 已迁移到 OrbStack；`apps/web/.env` symlink 需保留；provider 使用 `@ai-sdk/deepseek` 原生 provider
- **前端框架**：Vite 8 + React 19 SPA + react-router v7，路由在 `src/router.tsx`，页面在 `src/pages/`，布局在 `src/layouts/`
- **编辑器**：Monaco Editor（Catppuccin 暖色主题），IDE 布局（文件树 + Monaco + xterm.js 交互终端），通过 SandboxProvider 管理状态
- **沙箱**：OpenSandbox（Judge0 API 兼容），Hono Server 代理文件系统 + PTY WebSocket
- **左侧栏**：折叠态为图标栏（Logo/新建/消息/主题/设置/展开）
- **主题系统**：深色/浅色/跟随系统三种模式，ThemeProvider + ThemeToggle 在左侧栏
- **多模型**：后端 + 前端均已完成，设置页 `/settings`，支持 9 种 Provider
- **组件流式生成**：renderUI blocks 通过 `StreamingBlockParser` 逐个流式渲染
- **dev 命令**：`pnpm dev` 秒启动（Vite ~200ms），`pnpm fresh` 清 dist 重启
- **环境变量**：客户端变量用 `VITE_` 前缀（如 `VITE_API_URL`），服务端用 `process.env`
