# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：产品体验 7 问题分 3 组推进，已完成**第 1 组（两个 Bug 修复）+ 第 2 组（Agent Loop 健壮性迭代）**。第 2 组：①SSE 事件协议统一到 `packages/shared`（枚举+工厂+解析，消除散写）。②abort 信号链——server onAbort/control channel `chat:{id}:control` → worker AbortController → runAgentLoop 中断 + streamText abortSignal + `POST /api/chat/:id/abort` 路由，修复 stop() 不通知 worker。③循环检测——LoopDetector 纯函数（哈希指纹+ping-pong+熔断+只读白名单 TDD）集成到 runAgentLoop，注入提示+熔断。④failover 分层降级——LlmConfig 加 fallbackModelId/fallbackLlmConfigId 字段+迁移，runAgentLoop catch 触发 degradeStep（流式→非流式主 model→非流式 fallback model），Failover 事件降级前发出轨迹可见。⑤前端回答中禁用模型切换 + 设置页 fallback 配置。
- **文档同步**：`API接口.md`（abort 路由 + LlmConfig fallback 字段）、开发日志已同步；check:docs 全绿。待同步文档变更为空。
- **验证**：`pnpm test:unit`（192）、`pnpm lint`（0 errors）、`pnpm check:deps`、`pnpm check:docs`、`pnpm --filter @ai-teacher/web build` 全绿。E2E 待服务栈启动后统一跑（含 failover/循环检测/切换场景）。
- **下一步**：进入**第 3 组 过程可视化 + Token + 互动优化**（消息级折叠面板 + Token 双向圆 + explore 组件库 5 种），plan 已写在 `docs/superpowers/plans/2026-06-30-过程可视化与token与互动优化.md`。注意 Plan3 Task1 会补 step-start/end/reasoning 事件（Plan2 审查中提到的 step-end 缺口是 Plan3 的内容）。
- **注意**：Plan2 审查遗留 Minor（circuitBroken 未 await response；degradeStep 降级时 renderUI 流式 UI 块退化为整块——前端需兼容纯 UiBlocks）。分支 `feat/agent-loop-experience-and-robustness`。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
