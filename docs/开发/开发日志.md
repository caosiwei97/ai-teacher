# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：/learn 会话进入体验优化——VT API 真位移下沉 + 消除空态闪烁 + 首条消息格式化 + 输入框 icon 避让（2026-06-30，全门控绿）。
  - **下沉动画**：ChatInput 外层 div 标记 `view-transition-name: chat-input-shell`，LandingView.sendMessage 用 `document.startViewTransition` 包裹 navigate，输入框作为同一 DOM 元素从居中平滑下滑到底部（真位移，替代上轮整页淡出+淡入伪过渡）。旧浏览器降级为直接 navigate。移除了上轮 `leaving` 整页淡出。
  - **消除空态**：新增 `pendingFirstMessage` 标志（初值随 firstMessage），进入 ChatView 待发送首条消息期间（fetchSession 完成前）传「正在发送…」脉冲点占位 welcomeContent，抑制 ChatArea「开始你的学习之旅吧」fallback 闪烁。不乐观插入消息（避免与 handleSubmit 消息追加冲突）；submitMessage 同步 setMessages 使 messages 非空后清除 pending。
  - **消息格式化**：sendMessage 内 `formatFirstMessage` 统一包装为「请教我学习《》」，正则去重（已含 `《》` 或以"请教/学习/我想学"开头不重复包装）。
  - **icon 避让**：textarea padding `py-4`→`pt-4 pb-12`，多行时最后一行停在 icon 区上方（icon 区 `bottom-2`+40px=48px）。单行高度从 ~58px 涨到 ~90px，若实测过高可改 `pb-10`。
  - 全门控：typecheck(5包)/build/lint(0err)/test:unit(188)/check:deps/check:docs 全绿；E2E 断言审查无回归（home.spec/learn.spec 不受影响，未实跑）。
- **下一步**：本次需真人走查（`/learn` 点卡片 → VT API 输入框下沉 → `/learn/:id` 显示「请教我学习《》」+ loading → 诊断流 全链路；多行输入 icon 避让；手输去重）。重点验证 VT API 在 Chrome 实际动画效果——若动画不触发（React 并发模式快照时序），降级方案已备（功能正常）。后续 2.1.0 面试语音（054）。
- **注意**：落地页与聊天页合并为单组件 learn.tsx（壳 + LandingView + ChatView）；路由 `/`→重定向 `/learn`、`/learn`(无 id=引导态)、`/learn/:id`(聊天态)；发消息才建会话；VT API 下沉动画依赖 `document.startViewTransition`（Chromium 124+/Safari 18+），旧浏览器降级；pendingFirstMessage 时序：fetchSession 前 messages=[] 显示占位，isNewSession=true 后 submitMessage 使 messages 非空；历史孤儿空会话用 `pnpm --filter @ai-teacher/db cleanup-empty-sessions` 清理；遗留：产物聚焦面板增强 + 互动卡 uiBlocks 流式渲染偶发不显示（需查 use-chat-stream）+ 会话批量删除（用户提及，未做）+ 标题生成失败无手动重命名入口；053 产物聚焦面板降级（决策4）；playwright `workers:1`。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
