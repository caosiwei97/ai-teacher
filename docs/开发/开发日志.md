# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 046 完成（Phase 1 索引：4 个 model 添加 6 个 @@index 覆盖高频查询路径，迁移 20260626090416_add_query_indexes 已应用；Phase 2 Enum 已由 042 完成）
- **下一步**：048（依赖方向治理 — 归位 5 处 server↔worker 共享代码，先决策 provider-registry/profile-service/diagnostic 归属包）/ 047（Redis/MinIO 闭环）
- **注意**：1.1.0 已完成 042/043/044/045/046，仅剩 047/048；check-deps.mjs 的 KNOWN_VIOLATIONS 5 项待 048 清空；E2E 未跑（Node.status 值已在 E2E 同步改名，发版前需回归）
