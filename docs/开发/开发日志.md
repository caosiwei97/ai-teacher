# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：/learn → /learn/:id 进入对话体验已二次修复（2026-06-30 16:13）：输入框改为落地页/聊天页同款 composer，聊天页额外显示「推荐回答」icon；点「个人投资理财入门」后直接显示「请教我学习《个人投资理财入门》。」+ loading，不再闪「开始你的学习之旅吧」。
- **验证**：`pnpm -r typecheck`、`pnpm --filter @ai-teacher/web build`、`pnpm lint`(0 error)、`pnpm check:deps`、`pnpm check:docs`、`pnpm test:unit`(188)、`pnpm test:e2e e2e/learn.spec.ts`(12 passed) 全绿；learn.spec 新增首条消息、聊天/落地页 composer 差异、多行输入工具栏避让断言。
- **下一步**：真人走查 Chrome 上共享元素下沉动画手感（React Router `viewTransition + flushSync`，`chat-input-shell`）；继续后续候选：产物聚焦面板增强、互动卡 uiBlocks 流式偶发不显示、会话批量删除、标题手动重命名、2.1.0 面试语音（054）。
- **注意**：E2E 过程中仍会打印既有 `[backfill] 回填失败（不阻塞）` Prisma FK warning，不影响本次测试结果；待同步文档变更为空。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
