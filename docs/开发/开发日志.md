# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 048 完成（1.1.0 收官）— 5 处 server↔worker 依赖方向违规全部归位到 packages/shared/services/：provider-registry（3 处迁 shared 子路径）/ profile-service（1 处迁 shared，破叶子包约束）/ diagnostic（1 处 3a 重构，提炼 executeWithRetry 到 shared retry 工具，删死代码 BaseAgent）。check:deps 零违规零豁免。**1.1.0 工程化治理全部完成（042-048）**
- **下一步**：1.2.0 学习资料 + RAG（迭代 009，P3）— 启动时一并实施 047 Phase 2（MinIO 存储）+ Phase 3（视评估）；或可先发版跑 E2E 全量回归（042/048 改动较大）
- **注意**：E2E 未跑（042 Node.status 改名 + 048 diagnostic 重构，发版前必须跑分类 A 诊断 + 全量回归）；shared 现含 ai SDK 依赖但经子路径隔离未进 web bundle（已验证 build 体积一致）；047 Phase 2/3 暂缓待 009
