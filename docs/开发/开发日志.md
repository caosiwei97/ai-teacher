# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：落地页与 learn 页合并为单组件——`/learn` 引导态 + 输入框下沉动画（2026-06-30，全门控绿 + E2E 70/70 不回归）。
  - **需求**：默认路由改为 `/learn`（无 id 引导态），开始聊天后 URL 变 `/learn/:id`；聊天输入框是同一组件，发消息后自动下沉到底部（带淡出/淡入过渡动画），同时隐藏引导文字和标签。
  - **合并**：删 home.tsx，路由 `/`→重定向 `/learn`、`/learn`（无 id）、`/learn/:id` 三者共用 learn.tsx。learn.tsx 重构为壳组件 `Component`（按 `useParams().sessionId` 分发）+ `LandingView`（引导态，居中 ChatInput + 大标题 + 推荐 chips + 三阶段示意，从 home.tsx 迁移）+ `ChatView`（聊天态，原 learn 主体逻辑）。
  - **关键技术**：`useChatStream(sessionId)` 强依赖 sessionId，hooks 不能条件调用 → 用壳组件按有无 sessionId 渲染两个内层组件（LandingView 不调 useChatStream，ChatView 才调）。
  - **下沉动画**：LandingView 发消息时切 `leaving` 过渡态（opacity 0 + translate-y-4 淡出），建会话完成后 navigate 到 ChatView（输入框在底部淡入）。跨组件无法做同一 DOM 元素位移，用淡出+淡入过渡模拟。
  - **隐藏引导**：ChatView 的 isNewSession 分支不再传 welcomeContent（hero/标签已在 LandingView 展示），ChatArea 用默认 fallback 兜底空态。
  - **其他**：session-context createNewSession/archiveSession 跳转 `/`→`/learn`；learn.tsx pageError 返回首页按钮 `/`→`/learn`；删 handleTopicClick（welcomeContent 移除后无引用）。
  - 全门控：typecheck(5包)/build/lint(0err) 全绿；E2E 70/70 不回归（interview 偶发失败与本次无关，单独重跑通过）。
- **上轮进度**：新对话设计重构——「发消息才建会话」+ 左栏分类折叠 + 异步标题生成（2026-06-30，全门控绿 + E2E 70/70 不回归）。
  - **核心理念**：只要用户没发送消息就不应该产生对话 id。把「落地页建会话→跳转→发消息」三步改为「`/`页输入消息发送时才建会话→跳转接续流」，从根上消除孤儿空会话、首屏闪烁。
  - **后端**：
    - chat.ts 删除兜底创建会话逻辑（原 session 不存在自动创建），改为返回 404 暴露前端漏建。移除 DEFAULT_USER_ID 常量 + teachingMode 请求体字段（改从 session.teachingMode 取）+ session! 非空断言。
    - worker chat-turn.ts 新增 `generateSessionTitle`：首条消息处理完成后（isDiagnosisPhase && 无节点 && topic==="未命名对话"）调 generateText 生成 ≤10 字标题 → prisma.session.update → 发 `title-updated` SSE 事件。try/catch 兜底，失败保留「未命名对话」不阻断。
  - **前端**：
    - home.tsx `startLearning` → `sendMessage`：createSession(userId, "未命名对话") 拿 id → navigate 带 `firstMessage`（替代旧 `topic` state）。输入框 placeholder 改「输入你想学的，或直接提问」。
    - learn.tsx `startupTopic` → `firstMessage`：自动触发 effect 用 firstMessage 立即 submitMessage（无空态闪烁）；删除冗余 optimisticUpdateTopic(firstMessage)；isNewSession 分支去掉与落地页重复的 hero welcomeContent（改精简引导）；新增 onTitleUpdate 回调更新左栏标题。
    - use-chat-stream.ts 新增 `onTitleUpdate` 选项 + `title-updated` 事件分支（主流 + resumeStream 两处）。
    - session-context.tsx 新增 `newSessionHint` state：createNewSession 在 0 对话时提示「当前已是新对话」不跳转（2.5s 自动清除），有对话时跳 `/`。
    - app-layout.tsx：所有页面都显示「新对话」按钮（去掉 `/` 页隐藏逻辑），透传 newSessionHint。
    - left-sidebar.tsx：删除死分类 newSessions（status==="new" 永不出现）；分类改为「学习中」/「已完成」+ 可折叠（ChevronDown/Right + collapsedGroups state）+ 空分类显示「暂无对话」+ 标题右侧显示数量；新对话按钮下显示 newSessionHint。
    - three-column.tsx 透传 newSessionHint。
  - **清理脚本**：packages/db/prisma/cleanup-empty-sessions.ts + `db cleanup-empty-sessions` script，删除 0 消息 active/diagnosing 孤儿会话（上一轮「先建后端」遗留）。
  - **文档同步**：API接口.md 删「自动创建会话」章节改 404 说明 + 加 title-updated 事件 + POST /api/sessions 补发消息才建会话语义 + 版本 v1.2→v1.3；技术架构.md 加「会话生命周期」章节。
  - 全门控：typecheck(5包)/build/test:unit(188)/lint(0err)/check:deps/check:docs 全绿；E2E 70/70 不回归（home.spec 新增分类折叠用例 + placeholder 定位修正；mode-switch 灰锁用例补 cleanup）。
- **上轮进度**：去掉空对话 id 机制，改为「先建后端再跳转」（2026-06-30，全门控绿 + E2E 69/69 不回归）。
- **上轮进度**：053 人工走查 BUG 修复（2026-06-30，5 BUG 全修复 + 全门控绿 + E2E 69/69 不回归）。
  - BUG1 删完对话无法新建：`createNewSession` 用 `window.history.replaceState` 不触发 Router 重渲染 → 改 `navigate`；`archiveSession` 删完所有会话不再创建本地空会话，改回落地页 `/`。
  - BUG2 / 与 /learn/:id 首屏不一致：learn.tsx 新会话 welcomeContent 从旧"你好我是 AI Teacher"改为落地页式首屏（"真正掌握，而不只是看过" + 推荐主题 chips），与 `/` 统一。
  - BUG3 起步页+诊断题重复：删除 StartupCard 起步页（2 题点选），落地页输入主题后 `location.state.topic` 由 effect 自动 `submitMessage` 触发 agent 诊断流（3-5 题，已有自由输入）。消除重复问卷。
  - BUG4 诊断提交后无反馈：`diagnosticAnalyzing` 期间在 ChatArea 消息列表后加全局 loading banner（"正在为你定制学习路线…" + spinner），路线图生成后消失。
  - BUG5 还没学就掌握：tutor.ts 诊断流第4步 + toolCallingRulesSection 强化"必须先完成至少 1 轮教学互动才能 assessMastery，绝不能路线图生成后直接 assessMastery 跳过教学"。Prompt设计.md 同步。
  - 顺带清理：tutor.ts interviewer 死代码（前端已收窄 warm|strict，面试走独立 interview 路径，分支 100% 不可达）+ tutor.test.ts 对应用例；E2E README 用例数合计 56→69（实际）+ M/N/O/P 用例数补全。
  - 全门控：typecheck(5包)/build/test:unit(188)/lint(0err)/check:deps/check:docs 全绿；E2E 69/69 不回归（home.spec 断言更新：首屏文案"你好我是 AI Teacher"→"真正掌握"）。
- **上轮进度**：模型列表动态拉取改造（2026-06-30）。
  - **背景**：上轮修复 glm-4.7 后，发现静态维护的模型列表滞后（智谱实际 8 个模型含最新 glm-5.2，静态只有 5 个）。用系统里两个真实可用 key 实测 DeepSeek/智谱 `/models` 端点均返回 200 + 标准 OpenAI 格式 `{data:[{id}]}`，确认动态拉取可行。
  - **改造**：模型选择从"静态预设"改为"动态拉取为主 + 静态元信息补充 + 失败回退"。
    - 后端新增 `POST /api/llm/models/live`：传 {provider, apiKey, baseUrl?}，后端 fetch 供应商 `/models`，返回 `{success, models:string[]}`；失败按状态码映射中文提示 + detail 字段。
    - 前端 ModelSelector 重构：props 加 apiKey/baseUrl；apiKey 非空时动态拉取 id 列表，用 PROVIDER_PRESETS 按 id 匹配补 label/tier/price（匹配不上给默认 standard）；失败回退 getProviderModels 静态列表 + 黄色警告条。
    - LlmConfigForm 第 3 步传 apiKey/baseUrl 给 ModelSelector。
  - **决策**：① apiKey 明文传后端（与创建配置时一致）；② 拉取失败回退静态列表 + 警告（不阻断流程）；③ PROVIDER_PRESETS 保留作元信息补充 + 兜底，不删除。
  - 文档同步：API接口.md 新增 14.7 动态拉取可用模型 + 路由表。全门控：test:unit/lint/check:deps/check:docs/web build 全绿。
- **上轮进度**：智谱 glm-4.7 测试报 "not found" 排查与修复（2026-06-30）。
  - **根因**：`provider-registry.ts` 的 `default` 分支用 `createOpenAI()`，而 `@ai-sdk/openai` v3 的 `provider(modelId)` 默认走 **Responses API**（`/responses` 端点）；智谱/通义/Kimi/MiniMax/小米等国产 OpenAI 兼容供应商只实现 `/chat/completions`（GitHub issue zai-org/GLM-5#39 证实智谱不支持 /responses），故上游返回"模型不存在/not found"。DeepSeek provider 默认走 chat 故不受影响。
  - **修复①（调用规范）**：`default` 分支 `openai(modelId)` → `openai.chat(modelId)`，走 `/chat/completions`。对所有走 default 的供应商（zhipu/qianwen/kimi/minimax/xiaomi/custom）生效，worker 端 `chat-turn.ts:82` 同源调用自动受益。
  - **修复②（错误处理）**：`/test` 接口原 catch 只返回 `err.message`，丢弃 `APICallError` 的 statusCode/responseBody/url。改为 `formatTestError` 提取字段 + 中文映射（401→密钥无效、400含模型→模型名错误、404→Base URL 错误、429→限流、5xx→上游异常、无状态码→网络问题）+ `detail` 字段透传原始信息。前端 settings.tsx 失败时展示主消息 + `<details>` 可折叠详情（状态码/请求地址/原始响应）。
  - **附带**：智谱模型 `glm-4.7-flash` → `glm-4.7-FlashX`（官方文档确认 GLM-4.7 系列轻量版名带 X）。
  - **产品决策**：API 规范按 provider 自动推断，不暴露给用户（anthropic 走 Anthropic 规范，其余走 OpenAI Chat Completions）。
  - 文档同步：API接口.md 14.5 测试配置更新响应格式 + 错误映射说明。全门控：test:unit(188)/lint/check:deps/check:docs/web build 全绿。
- **上轮进度**：2.0.0 收官后的互动卡体验性 bug 修复（2026-06-29，4 问题全修复 + 实测验证 + 全门控绿 + E2E 16/16 不回归）。
  - ①滑块不响应：根因是互动卡 `<script>` 内 feedback 字符串单引号嵌套致 `SyntaxError`（整个 script 不执行，addEventListener 未注册）。修复：tutor.ts lessonSection 加 HTML 硬性规范（交互用 addEventListener 禁 inline on\*；JS 字符串用反引号）+ 正反例对照。实测：node --check 无 SyntaxError，滑块拖动数字响应式更新（300→0）。
  - ②自测选项挤一起：根因是 agent 生成 `<button onclick="checkAnswer('a")'>`（引号错位）+ sanitize 正则贪婪匹配产生畸形 `<buttona>`。修复：同 prompt 规范（选项用 button + addEventListener）。实测：选项正常 button 不挤、点击有反馈。
  - ③输入框间距：ChatInput `p-4`→`p-5`（上下左右 20px）+ chat-area 滚动区 `py-6`→`pt-6 pb-8`（对话区底部留间距）。
  - ④折叠 icon 挡标题：mode-tabs "已掌握"文字加 `lg:pr-12` 避让折叠按钮，实测 overlap=false。
  - 文档同步：Prompt设计.md 互动课规范同步（骨架示例 + 正反例）。全门控：build/test:unit(189)/lint(0err)/check:deps/check:docs 全绿；E2E interactive-lesson/learn/mode-switch 16/16 不回归。
  - **关键经验**：prompt 规范"用反引号"agent 未必字面遵守，但正反例对照能让它避开引号嵌套冲突（核心问题），SyntaxError 消除。LLM 转义引号不可 100% 可靠，正反例对照比抽象规则有效。
- **下一步**：本次合并需真人走查（`/learn` 引导态输入消息 → 淡出 → `/learn/:id` 聊天态输入框下沉 → 诊断流 → 标题生成更新左栏 全链路 + 0 对话点新对话提示 + 左栏分类折叠/空态）。后续 2.1.0 面试语音（054，依赖 049 DashScope KEY）。遗留：产物聚焦面板增强 + 互动卡 uiBlocks 流式渲染偶发不显示（需查 use-chat-stream，高风险调查项）+ 会话批量删除（用户提及，未做）+ 标题生成失败时无手动重命名入口（本次未做）+ 输入框下沉动画目前是淡出淡入过渡（跨组件），若要真正同一 DOM 位移需 View Transitions API。
- **注意**：落地页与聊天页合并为单组件 learn.tsx（壳 + LandingView + ChatView）；路由 `/`→重定向 `/learn`、`/learn`(无 id=引导态)、`/learn/:id`(聊天态)；发消息才建会话（`/learn` 输入首条消息 → POST /api/sessions topic="未命名对话" + teachingMode → 跳 /learn/:id 带 firstMessage → 立即触发 chat 流）；chat.ts 不再兜底创建（404 暴露漏建）；首条消息后 worker 异步生成标题（generateText）+ 发 title-updated 事件；isNewSession 靠「无可见消息」判定；左栏分类按学习状态（学习中/已完成）+ 可折叠 + 空态「暂无对话」；0 对话点新对话提示「当前已是新对话」；历史孤儿空会话用 `pnpm --filter @ai-teacher/db cleanup-empty-sessions` 清理；BUG5 prompt 引导修复无运行期兜底，agent 偶尔仍可能跳过教学（需观察）；053 产物聚焦面板降级（决策4）；playwright `workers:1`。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
