# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：产品体验 7 问题分 3 组推进，**三组全部完成**。第 3 组（过程可视化 + Token + 互动优化）：①worker 产出 step-start/step-end/reasoning-delta 事件（reasoning-delta 读 `text` 字段，ai SDK v6 TextStreamPart 用 text 非 delta）。②前端消息级折叠过程面板 `loop-trace-panel.tsx`——展示 reasoning/工具调用（ToolCall 时写入当前 step 的 tools）/每步耗时/降级轨迹/循环警告，默认折叠。③Token 双向圆仪表盘 `token-meter.tsx`——worker streamText onFinish 读 totalUsage 发 Usage 事件 + ContextInfo 事件，前端 SVG 双向圆（左半 input 右半 output）+ 真实用量明细（缓存命中/思考，有则展示无则隐藏）+ 会话累计 + 上下文占比，provider 无关。④explore 组件库加齐 5 种（choice/matching/ordering/fill-blank/chart-slider），interactive-block 按 kind 分发，render-ui schema + tutor prompt 同步扩展。
- **文档同步**：API接口.md（abort 路由 + LlmConfig fallback）、开发日志已同步；check:docs 全绿。技术架构/Prompt设计/决策记录 ADR 待补（主 Agent 收尾）。
- **验证**：`pnpm test:unit`（192）、`pnpm lint`（0 errors）、`pnpm check:deps`、`pnpm check:docs`、`pnpm --filter @ai-teacher/web build` 全绿。E2E 待服务栈启动后统一跑。
- **下一步**：①补技术架构/Prompt设计/决策记录 ADR 文档；②真人走查验证 failover 降级轨迹、循环检测、过程面板、token 仪表盘、新 explore 组件的实际体验；③合并分支 `feat/agent-loop-experience-and-robustness`。
- **注意**：遗留 Minor——circuitBroken 未 await response；degradeStep 降级时 renderUI 流式 UI 块退化为整块（前端已兼容纯 UiBlocks）；Plan1 Task4 的 reader 未 cancel 释放 + diagnosticError setTimeout 竞态。estimatedSystemTools 暂传 0 标注估算（system/tools token API 不拆分）。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
