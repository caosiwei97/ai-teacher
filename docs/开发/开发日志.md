# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 1（迭代 050）子任务①**删掌握仪式 + prompt 改造 ✅ 完成**。assess-mastery instruction 四步仪式→一句推进（不生成报告/不庆祝/不复述）；tutor.ts 删规则5"2-3 轮总结"强制轮数 + 过渡说明精简；assess-mastery 单测 TDD 红→绿；Prompt设计.md 同步。Phase 0（049）已完成推送，详见 git log。
- **全门控**：typecheck ✅ / test:unit 118 ✅ / check:docs ✅ / lint 0 error / check:deps ✅ / web build ✅ / **E2E 57/57 ✅（19.2s）**。
- **环境备注**：本次 `pnpm install` 补装 pdf-parse/aws-sdk（解除 document-parser/storage 单测预存失败）；`prisma generate` 重生成 client（LlmConfig.source 类型）。youtube-dl-exec postinstall 因 GitHub rate limit 失败（009 视频功能依赖，不影响①）。E2E 仍有 worker 解密错误预存遗留（宽松断言绕过，非①引起）。
- **下一步**：子任务②HTML 沙箱渲染器（interactive block schema + iframe 沙箱 + render-ui 放开 + 净化 + 降级 + 既有债梳理），③形态 A 互动课依赖②。详见 `迭代详情/2.0.0/050-互动产物与学习模式改造.md`。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
