# AI Teacher — 开发日志

> 记录开发过程中的关键时间节点和决策，防止上下文溢出后丢失信息。
>
> 详细日志按日期归档到 `开发日志/YYYY/MM/` 子目录。本文件仅保留里程碑摘要 + 归档索引 + 继续点。
> 条目格式：事件概述（1-2 句）+ 关键决策（仅 WHY）+ 提交引用。代码变更详情见 `git log`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。格式见 AGENTS.md §3.5 -->
<!-- 下一个 session 启动时消费：更新文档 → 移除条目 → 记入当日日志 -->

---

## 里程碑时间线

### 2026-05-08 — 项目初始化 ~ 迭代 010

- 完成同类竞品深度拆解 → 项目骨架 → 技术栈选型（Next.js 15 + AI SDK + PostgreSQL）
- **迭代 001**：项目骨架 + Docker 环境（pnpm monorepo + 非标准端口 + Prisma）
- **迭代 002**：种子数据（test user + React Hooks 主题 + 5 节点 roadmap）
- **迭代 003**：苏格拉底 Agent 核心（智谱 glm-4-flash + 5 tools + SSE 流式）
- **迭代 004**：Chat UI + 三栏布局（shadcn/ui + 三栏 + 流式对话）
- **迭代 005**：知识图谱生成 + 可视化（Roadmap Agent + generateObject + Zod）
- **迭代 006**：诊断摸底（Diagnostic Agent + 选择题/简答题 + 起始节点定位）
- **迭代 007**：掌握度评分 + 门控（assessMastery + 自动推进下一节点）
- **迭代 008**：评估卡片渲染（总结 + 回顾表 + 核心标签 + "下一节"按钮）
- **迭代 010**：会话管理 + 历史记录（PATCH/DELETE + 仪表盘 + 归档）
- **迭代 014**：快问 + AI 建议回复（streamText + generateText）
- Bug 修复：Tailwind CSS 加载 + Chat API schema 不兼容
- UI 设计系统：暖色教育主题
- E2E 测试基础设施搭建
- Agent 引擎优化方案设计
- 开发体验优化：pnpm 强制 + 一键启动 + 环境自动化
- **详情**：[08.md](开发日志/2026/05/08.md)

### 2026-05-09 — Agent 引擎重构 + 首页重设计 + 架构升级

- **迭代 011**：Agent 引擎重构 Phase 1（BaseAgent + Service 层 + 工厂函数）
- **迭代 012**：Agent 引擎重构 Phase 2（消息截断 + 重试 + fallback）
- **迭代 013**：学习者画像 + 跨会话记忆
- 首页重设计 + 护眼配色全局改造
- **迭代 016**：架构拆分 Phase 1 — Next.js API Routes → Hono 独立服务
- **迭代 017**：架构拆分 Phase 2 — Worker 激活 + BullMQ 队列 + Redis Pub/Sub
- **迭代 018**：Agent 引擎 Phase 3 — StateGraph + ToolRegistry + CheckpointStore
- **迭代 019**：上下文管理升级 — ContextManager + AgentMessage 分层 + Compaction
- **迭代 020**：A2UI 框架 + 代码编辑器（CodeMirror 6 + KaTeX）
- **迭代 021**：代码执行沙箱 — Judge0 + executeCode 工具 + CodeResultBlock
- **迭代 022**：多 Agent 协作 — SubagentRegistry + delegateTask
- **迭代 023**：技术栈全面升级 — AI SDK v4→v6 + Zod 3→4（跨 6 包、30+ 文件）
- **详情**：[09.md](开发日志/2026/05/09.md)

### 2026-05-10 — E2E Mock + 迭代 024-029 + LLM 切换 DeepSeek

- **E2E Mock 策略**：MOCK_LLM=true 服务端 mock，24/24 测试 11 秒通过
- **LLM 切换**：智谱 → DeepSeek-V4-Flash + @ai-sdk/deepseek 原生 provider
- **迭代 024**：renderUI 工具 + 结构化教学组件（table/callout/comparison）
- **迭代 025**：右侧栏实时代码编辑器 + pushCode 工具
- **迭代 026**：Prompt 工程全面优化（教学模式、动态注入、stopWhen 增强）
- **迭代 027**：聊天内诊断重构（askQuestion + hidden 消息 + Tab 选择题 UI）
- **迭代 028**：SSE 事件扩展（roadmap-updated / session-updated 实时推送）
- **迭代 029**：掌握报告与自动节点过渡（MasteryReport + 自动桥接）
- **详情**：[10.md](开发日志/2026/05/10.md)

### 2026-05-11 — UI 重设计 v2 + 诊断增强 + 浅色主题 + 多模型

- **迭代 032**：UI 重设计 v2 — "Deep Focus" 设计系统（靛蓝主色 + Inter + JetBrains Mono）
- **迭代 033**：首页交互优化（textarea + 布局重排 + "学习"前缀）
- **迭代 034**：诊断摸底增强（5-10 题多维度 + 手动提交 + analyzing 动画）
- **迭代 036**：浅色主题 + 皮肤切换（ThemeProvider + ThemeToggle + 防闪烁）
- **迭代 031**：多模型支持前端 UI + 设置页（级联 3 步表单 + 模型切换）
- 流式响应延迟优化（首 Token 延迟降低 3-5 秒）
- **详情**：[11.md](开发日志/2026/05/11.md)

### 2026-05-12 — Bug 修复 + Prompt Pipe + OpenSandbox 迁移

- Bug 修复：卡片标题换行 + 诊断摸底顺序 + 流式渲染未闭合标记
- Prompt 系统重构为 Prompt Pipe 模式（8 个独立 section 函数）
- Bug 修复：学完小主题后不自动进入下一个（移除冗余工具 + 增加 step 预算）
- Judge0 → OpenSandbox 迁移（跨平台、Docker 隔离、API 兼容性修复）
- Bug 修复：切换/新建对话页面闪烁（SessionContext + 路由组 + replaceState）
- **详情**：[12.md](开发日志/2026/05/12.md)

### 2026-05-13 — 代码编辑器优化 + 沙箱增强 + 文档重整

- Bug 修复：右侧面板折叠后消失、编辑器清空后高度塌缩
- UI 优化：运行按钮移到工具栏 + Copy + Output 标题 + 行号/语法高亮
- 沙箱增强：注入 OPENAI_API_KEY，Agent prompt 更新
- 诊断增强：编程主题自动加语言偏好题
- dev 命令：`pnpm dev` 保留缓存，`pnpm fresh` 清缓存
- 文档结构重整：归档 2 个已实施方案、删除 12 个冗余文件、新增 3 个设计文档
- VSCode 式暗色 IDE 布局：Activity Bar + 文件 Tab + 统一 Catppuccin 暗色 + pushCode 自动建沙箱工程
- 沙箱容器生命周期管理：启动清理孤儿 + 退出销毁 + 复用已有容器
- **详情**：[13.md](开发日志/2026/05/13.md)

### 2026-05-15 — 迭代 038-039 收尾 + 迭代 040 完成

- **迭代 038 完成**：沙箱多文件工程结构全部功能验证通过
- Bug 修复：文件上传 multipart 格式适配（execd 需要 `metadata` + `file` 两个 part）
- Bug 修复：代理响应解析（execd 空 body 返回 `\n`，`JSON.parse` 需先 `.trim()`）
- 新增 `safeJson()` helper 统一所有代理路由的响应解析
- 前端 `useSandboxApi.uploadFile` 修复 FormData 格式
- 新增 E2E 测试 `e2e/sandbox-ide.spec.ts`（文件 CRUD + PTY + 代码执行）
- **迭代 039 完成**：组件流式生成（renderUI blocks 逐个流式渲染）
- **0.4.0 版本全部完成**（迭代 030-039）
- **迭代 040 完成**：Next.js → Vite + React 迁移
  - Next.js 15 整体替换为 Vite 8 + React Router v7
  - 构建时间 ~8s → 1.2s，dev 启动 ~8s → 212ms
  - App Router 文件路由 → react-router createBrowserRouter（3 页面 + 1 layout）
  - next/font/google → @fontsource-variable/{inter,jetbrains-mono}
  - Next.js API Routes（纯代理）→ Vite server.proxy
  - NEXT_PUBLIC_API_URL → import.meta.env.VITE_API_URL
  - 删除 50 个文件的 "use client" 指令、next.config.ts、next-env.d.ts
  - 50/53 E2E 通过（3 个沙箱超时为已知 flake）
- **详情**：[15.md](开发日志/2026/05/15.md)

### 2026-05-16 — 全量文档同步

- 文档审计：4 个并行 explore agent 验证代码与文档一致性
- 发现 8 个文件存在过时/错误信息，逐一修正
- **修正清单**：
  - `.env.example`：`NEXT_PUBLIC_API_URL` → `VITE_API_URL`
  - `README.md`（根）：Next.js → Vite、移除 `packages/agent/`、端口表、迭代数
  - `AGENTS.md`：移除 `packages/agent/` 行、Judge0 → OpenSandbox、`pnpm fresh`
  - `docs/README.md`：0.4.0 范围含 039、新增 1.0.0 目录
  - `docs/设计/技术架构.md`：全面重写 v1.5（编辑器 CodeMirror→Monaco、沙箱 Judge0→OpenSandbox、项目结构、Agent 架构去除 StateGraph/ToolRegistry 幻觉、Docker Compose、环境变量、Section 10 标记为已完成）
  - `docs/设计/API接口.md`：移除 Next.js 引用、`glm-4-flash` → `deepseek-v4-flash`
  - `docs/开发/迭代计划.md`：0.4.0 标题修正、"下一步"更新、1.0.0 状态修正
  - `docs/开发/开发日志.md`：日期排序修正、`pnpm dev:fresh` → `pnpm fresh`

---

## 日志详情按日期归档

| 日期 | 文件 | 主要内容 |
|------|------|----------|
| 2026-05-08 | [08.md](开发日志/2026/05/08.md) | 项目初始化 ~ 迭代 010 + 迭代 014 |
| 2026-05-09 | [09.md](开发日志/2026/05/09.md) | 迭代 011-013 + 架构升级 + 迭代 016-023 |
| 2026-05-10 | [10.md](开发日志/2026/05/10.md) | E2E Mock + 迭代 024-029 + LLM 切换 |
| 2026-05-11 | [11.md](开发日志/2026/05/11.md) | 迭代 031-034/036 + UI 重设计 + 流式优化 |
| 2026-05-12 | [12.md](开发日志/2026/05/12.md) | Bug 修复 + Prompt Pipe + OpenSandbox |
| 2026-05-13 | [13.md](开发日志/2026/05/13.md) | 迭代 037 + 文档重整 + Monaco Editor 架构升级 |
| 2026-05-15 | [15.md](开发日志/2026/05/15.md) | 迭代 038-040 + 0.4.0 收尾 + Vite 迁移 |

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 001-040 全部完成，1.0.0 版本进行中（文档已全面同步）
- **下一步**：迭代 041（Agent 引擎重构 — 删壳归位）或 迭代 009（学习资料上传 + RAG）
- **注意**：Docker 已迁移到 OrbStack；`apps/web/.env` symlink 需保留；provider 使用 `@ai-sdk/deepseek` 原生 provider
- **前端框架**：Vite 8 + React 19 SPA + react-router v7，路由在 `src/router.tsx`，页面在 `src/pages/`，布局在 `src/layouts/`
- **编辑器**：Monaco Editor（Catppuccin 暖色主题），IDE 布局（文件树 + Monaco + xterm.js 交互终端），通过 SandboxProvider 管理状态
- **沙箱**：OpenSandbox（Judge0 API 兼容），Hono Server 代理文件系统 + PTY WebSocket
- **左侧栏**：折叠态为图标栏（Logo/新建/消息/主题/设置/展开）
- **主题系统**：深色/浅色/跟随系统三种模式，ThemeProvider + ThemeToggle 在左侧栏
- **多模型**：后端 + 前端均已完成，设置页 `/settings`
- **组件流式生成**：renderUI blocks 通过 `StreamingBlockParser` 逐个流式渲染
- **dev 命令**：`pnpm dev` 秒启动（Vite ~200ms），`pnpm fresh` 清 dist 重启
- **环境变量**：客户端变量用 `VITE_` 前缀（如 `VITE_API_URL`），服务端用 `process.env`
