# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 2（迭代 051 复习模式）🔧 进行中。①② ✅ 完成。①数据层+间隔重复算法 TDD；②复习 Agent+API：考官 prompt（review.ts）+ recordReviewResult tool + chat-turn 按 activeMode 分流 + ReviewService（getDueNodes/submitResult/getSummary）+ review API（/due /result /summary）+ flashcard block + PATCH activeMode。dev 真实 LLM 走查通过（考官风格+提取练习不重讲+renderUI flashcard+DB 记忆强度更新）。全门控绿 / E2E 58/58。详见迭代详情 051。
- **下一步**：051 子任务③ 复习 UI + E2E。右栏复习清单（今日到期，黄色基调）+ 抽认卡翻面/回忆测验交互 + 提交结果回写（POST /review/result + 切 activeMode=review）+ review.spec.ts（含单点降级/≥2 交错两场景，spec §9.2）。详见 spec §3.2/§5.1 + 051 详情③。
- **注意**：③ 需加 FlashcardBlockRenderer（registry）；review.spec.ts mock LLM 走查复习流程；recordReviewResult 端到端在 ③ E2E 完整覆盖。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
