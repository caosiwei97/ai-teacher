# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 044 完成（缓存与重启.md 重写 Vite+Hono + 文档同步规则加 references 触发 + check:docs 第 6 项 references 存在性检查）
- **下一步**：1.1.0 剩余项（042 Linter+依赖方向 P0 / 046 Schema 索引 / 047 Redis-MinIO）
- **注意**：042 是大迭代（ESLint 配置重写 + Prisma Enum migration），需单独 session
