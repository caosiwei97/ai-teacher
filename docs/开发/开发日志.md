# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 4（迭代 053 UI 统一工作台）① ✅ **已完成**（2026-06-29）。顶部三模式 Tab（`mode-tabs.tsx`，学靛蓝/固黄/验红主色）+ Tab 驱动 activeMode 切换（`handleModeChange`：切复习/面试触发首轮，切回 learning 不发消息）+ 渐进解锁（mastered=0 灰锁+Lock+hover 提示）+ `right-sidebar` tab 退化（学习保留 roadmap/code/interactive，复习/面试固定 panel）+ `globals.css` 新增 `--color-review`/`--color-interview`（不动 accent）+ `mode-selector` 收口（移除 interviewer 项，TeachingMode 收窄 `warm|strict`，4 处类型同步）。全门控绿（typecheck/lint 0 error/check:deps/test:unit 189/check:docs/build）。UI 行为验证留 ③ `mode-switch.spec.ts` E2E。详见迭代详情 053。
- **下一步**：053 ② 落地页 + 起步页 + 左栏改造。`home.tsx` 重写为落地页（大标题+输入框+chips+三阶段示意，替代纯重定向，保留 Phase 0 软引导）；learn 页起步页状态（`isNewSession && !started`，2 问题可跳过，不改路由）；`left-sidebar` 模式图标/进度色/新对话提权/完成态 ✓（需 session 列表带 activeMode，可能补 `fetchSessions` 字段）。② E2E 会红，留 ③ 修。
- **注意**：053 ③ 超宽屏产物聚焦面板 + E2E 重写（learn/session-management/quick-features + 新增 `mode-switch.spec.ts` 分类 T）。`tutor.ts:31` interviewer 死代码分支记既有债（053 不动 worker）。语音 054 依赖 2.1.0。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
