# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：迭代 047 完成（仅 Phase 1 评估+文档修正：核查发现 Redis 实际是 BullMQ 队列 + Pub/Sub 流式推送骨干而非"缓存预留"，修正技术架构.md 4 处自相矛盾表述；MinIO/pgvector 保留为 009 准备；Phase 2/3 暂缓见 ADR-026）
- **下一步**：048（依赖方向治理 — 归位 5 处 server↔worker 共享代码，先决策 provider-registry/profile-service/diagnostic 归属包，1.1.0 最后一项）
- **注意**：1.1.0 已完成 042/043/044/045/046/047，仅剩 048；check-deps.mjs 的 KNOWN_VIOLATIONS 5 项待 048 清空；E2E 未跑（Node.status 值已在 E2E 同步改名，发版前需回归）；047 Phase 2（MinIO 存储）/Phase 3（Redis 缓存）暂缓，待迭代 009 RAG 启动时实施
