# AI Teacher — 开发日志

> 本文件仅用于 session 断点恢复。项目历史见 `git log`，迭代状态见 `迭代计划.md`。

## 待同步文档变更

<!-- Agent 改了代码但未更新文档时，在这里追加条目。下一个 session 启动时消费 -->

---

## 下次 Session 应从这里继续

- **当前进度**：2.0.0 Phase 4（迭代 053 UI 统一工作台）①② ✅ **已完成**（2026-06-29）。①顶部 Tab+渐进解锁+三模式主色；②落地页（`home.tsx` 重写：大标题+输入框+chips+三阶段示意+继续上次入口，替代纯重定向）+ 起步页（`startup-card.tsx`，2 问题可跳过；learn 页 `isNewSession && 有 topic state` 显示，提交/跳过走对话流——**偏离**：纯前端 UI 前置，完整替代诊断仪式需改 agent，留后续）+ 左栏改造（模式图标🌱/🔁/🔥+进度色随模式+新对话提权+完成态✓）+ session activeMode 字段链路（server 响应+api-client+session-context，`API接口.md` 已同步）。全门控绿。UI 行为留 ③ E2E。
- **下一步**：053 ③ 超宽屏产物聚焦面板 + E2E 重写。xl/2xl（≥1440）对话区裂栏（对话流限宽 680 + 产物聚焦面板，互动课展开图标）+ 右栏折叠浮标（🗺 路线 mastered/total 摘要）；重写 `learn.spec.ts`/`session-management.spec.ts`/`quick-features.spec.ts`（入口+布局变更）+ 新增 `mode-switch.spec.ts`（分类 T：Tab 切换 URL 不变 + 渐进解锁两态 + 响应式 `setViewportSize` 三断点）+ 同步 E2E 矩阵。
- **注意**：③ 是 053 收官，E2E 影响面最大（①② 改入口/布局导致 learn/session-management/quick-features 红，③ 统一修）。`tutor.ts:31` interviewer 死代码记既有债。语音 054 依赖 2.1.0。

---

## 历史断点（1.2.0 / 009）

- **009 Phase 1（文本类 RAG）**：✅ 全部完成并验证（M1/M2/M3，E2E 54/54）。
- **009 Phase 2 V1（视频理解 spike）**：✅ 完成并真实验证。架构：视频字节 → DashScope 文件 API → `oss://` URL → Qwen3.5-Omni → digest → 复用 RAG。38MB YouTube 实测准确。
- **009 Phase 2 V2（视频 worker 全链路）**：🔧 进行中。下一步：worker video 分支 + sources 路由 video 上传/URL + shared schema（SourceType+=video, externalUrl）。V2 的迁移文件时间戳 bug 已修复（commit cd3b1d3，改名 `20260626170000`）。B站需 SESSDATA。`source.ts` 的未提交改动属于 V2。
- **已知遗留**：E2E worker 的 Prisma 连接系统性报 "Server has closed the connection"（宽松断言绕过，根因待查）。
