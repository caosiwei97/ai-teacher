# 050 — 互动产物能力 + 学习模式改造（2.0.0 Phase 1）

> 状态：🔧 进行中（子任务①，2026-06-29） | 分类：🔵 功能 | 优先级：P0 | 依赖：049（Phase 0）
> 设计 spec：[docs/superpowers/specs/2026-06-27-教学模式重设计-design.md](../../../superpowers/specs/2026-06-27-教学模式重设计-design.md)

## 目标

把"对话主导、卡片点缀"翻转为"互动教学产物主导、对话辅助"。本迭代交付 HTML 沙箱渲染器 + 形态 A 互动课 + 教练式 prompt 改造 + 删掌握仪式，兑现核心量化目标：单知识点对话轮次 4-6 → 1-2。

## 子任务拆分（spec §8 + §9.2）

| 子任务                             | 内容                                                                                     | 依赖 | 风险 | E2E 影响                   |
| ---------------------------------- | ---------------------------------------------------------------------------------------- | ---- | ---- | -------------------------- |
| ① 删掌握仪式 + prompt 改造（减法） | assess-mastery 四步→一句 + tutor.ts 删规则5强制总结 + 过渡说明精简                       | 无   | 低   | mastery/chat               |
| ② HTML 沙箱渲染器（基础设施）      | interactive block schema + iframe 沙箱渲染器 + render-ui 放开 + 净化 + 降级 + 既有债梳理 | 无   | 中   | 新增 interactive-lesson    |
| ③ 形态 A 互动课落地（加法）        | 教练式 prompt 加互动课产出指引（三段式）+ 规则1铺垫放宽 + 调用时机放宽                   | ①+②  | 中   | interactive-lesson + learn |

> ①② 互不依赖可并行；③ 依赖 ①+②。本次先做①。

## spec 偏离记录（实施纪律 §4）

spec §2.3 列三点 prompt 改造：放宽规则1铺垫、删规则5强制轮数、精简仪式。

- **① 已做**：删掌握仪式（assess-mastery instruction 四步→一句）+ 删规则5"2-3 轮总结"强制轮数（含配套的 toolCallingRules 调用时机"每 2-3 轮"→"理解到位即"）+ 过渡说明精简
- **移到③**：规则1铺垫放宽（依赖产物承担讲解，①无产物若删铺垫则概念无来源、教学突兀）

> "删规则5强制轮数"自然带动 toolCallingRules 调用时机措辞从"每 2-3 轮"改为"理解到位即"——两者配套，非独立偏离。产物落地后（③）进一步收敛到 1-2 轮（spec §2.2 对话量目标）。

## 子任务① — 删掌握仪式 + prompt 改造

### instruction 消费链核查（实施前）

- `assess-mastery.ts:81-87` 有 nextNode 返回四步 instruction（renderUI 报告→庆祝→桥接→STOP）；`:97` 结业 instruction
- `run-agent-loop.ts:208` 的 `instruction` 是 **pushCode** 的，**不涉及 assess-mastery** → 改 assess-mastery instruction 不影响 SSE 透传
- `tutor.ts:136-138` prompt 指示 agent"严格按 instruction 步骤执行后停止"
- instruction 仅 LLM 消费，无其他代码依赖 → 改文本安全

### 实施 checklist

- [x] [TDD红] 更新 assess-mastery 单测 instruction 断言为"删仪式后"期望（不再含 STOP 四步/不要求 renderUI 报告；含下一节标题 + 推进语义；结业精简）→ 文件: `apps/worker/src/agent/tools/assess-mastery.test.ts` | 验证: `pnpm test:unit -- assess-mastery` 红
- [x] [TDD绿] assess-mastery.ts instruction 改造：有 nextNode 四步→一句推进（确认+预告下一节+停止，不生成报告/不庆祝/不重复概念）；结业→一句简短祝贺 → 文件: `apps/worker/src/agent/tools/assess-mastery.ts` | 验证: `pnpm test:unit -- assess-mastery` 绿 | 影响: Prompt设计.md(工具定义)
- [x] tutor.ts prompt 改造：规则5"追问2-3轮后总结"→"确认理解即推进，不每轮总结复述"；toolCallingRulesSection 过渡说明精简（删"严格按步骤"+强调不生成报告不庆祝）→ 文件: `apps/worker/src/agent/prompts/tutor.ts` | 验证: typecheck | 影响: Prompt设计.md(prompt模板)
- [x] 同步 Prompt设计.md：assess-mastery 工具 instruction 语义 + tutor prompt 规则5/过渡说明 → 文件: `docs/设计/Prompt设计.md` | 验证: `pnpm check:docs`
- [x] E2E 回归：确认 mastery.spec.ts/chat.spec.ts 现有用例不破坏（删仪式强断言因真实 LLM 触发掌握不稳定，纳入人工验收）→ 文件: `e2e/mastery.spec.ts`,`e2e/chat.spec.ts` | 验证: `pnpm test:e2e -- mastery chat` 绿
- [x] 全门控：typecheck/lint/check:deps/test:unit/check:docs/web build → 验证: 各命令通过
- [x] 全量 E2E 回归 → 验证: `pnpm test:e2e` 57/57 绿
- [x] 文档收尾：迭代计划.md(050 🔧) + 开发日志.md(下次继续) + 本文件 checklist 勾选

### 验收标准（子任务①）

- [x] assess-mastery instruction 不再含四步仪式（renderUI 报告/庆祝/桥接/STOP），改为 ≤1 句推进（单测断言）
- [x] tutor.ts 不再强制"2-3轮总结"（typecheck + prompt 文本核对）
- [x] 现有 E2E（mastery/chat）不破坏，全量 57/57 绿
- [x] 全门控通过
- [ ] 人工：学一个知识点，掌握后 agent 不再输出总结报告+庆祝长文（spec §9.3 啰嗦度走查）

### E2E 策略说明

spec §9.2 Phase 1 要求"E2E 断言掌握后回复字符数/不含固定仪式文本"。但删仪式的效果是 LLM 根据 instruction 生成的文本，E2E 需真实 LLM 多轮对话触发 assessMastery 掌握，慢且不稳定（本项目 E2E 对 LLM 行为断言弱，chat.spec 仅断言"收到回复"）。故：

- 单测层稳定覆盖 instruction 文本变更（TDD）
- E2E 层只确保现有用例不破坏
- 删仪式实际效果纳入人工验收（spec §9.3 已列"对话啰嗦度"为人工指标）

## 风险

- instruction 改中文可能影响其他依赖 instruction 文本的逻辑 → 已核查消费链，仅 LLM 消费，安全
- 删仪式后 agent 行为变化需人工走查确认不突兀
