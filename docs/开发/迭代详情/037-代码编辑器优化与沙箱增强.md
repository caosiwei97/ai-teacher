# 迭代 037 — 代码编辑器优化 + 沙箱增强

> 状态：✅ 已完成 | 日期：2026-05-13

## 目标

优化右侧栏代码编辑器的交互体验，增强沙箱执行能力。

## Checklist

- [x] 运行按钮从底部全宽改为工具栏图标按钮（Play icon）
- [x] 工具栏新增复制代码按钮（Copy icon）
- [x] 清空后编辑器高度塌缩 bug 修复（CodeMirror 容器加 min-h）
- [x] 编辑器启用行号 + 语法高亮 + 括号匹配 + 当前行高亮
- [x] 输出区域加 "Output" 小标题 + 执行耗时
- [x] 右侧面板折叠后无法展开 bug 修复（响应式类写反）
- [x] 沙箱注入主应用 LLM API Key（OPENAI_API_KEY + OPENAI_BASE_URL）
- [x] 沙箱 API Key 注入带安全警告注释（TODO: Web 服务时改 API 代理）
- [x] Agent prompt 更新：沙箱有 key 可直接调用 openai，缺库先 pip install
- [x] 诊断摸底：编程主题自动加语言偏好题（Python/JS/TS/Java/C++）
- [x] dev 脚本优化：`pnpm dev` 保留缓存，`pnpm dev:fresh` 清缓存

## 代码变更

| 文件 | 变更 |
|------|------|
| `apps/web/src/components/sidebar/code-panel.tsx` | 运行按钮移到工具栏 + 新增复制按钮 + Output 标题 |
| `apps/web/src/components/editor/code-editor.tsx` | 启用行号/高亮/括号匹配 + min-h 防塌缩 |
| `apps/web/src/app/(app)/learn/[sessionId]/page.tsx` | 右侧面板折叠 bug 修复 |
| `apps/server/src/services/sandbox.ts` | 沙箱注入 API Key + 安全注释 |
| `apps/worker/src/agent/tools/execute-code.ts` | prompt 更新：沙箱有 key |
| `apps/worker/src/agent/tools/ask-question.ts` | 编程主题加语言偏好题 |
| `apps/worker/src/agent/prompts/tutor.ts` | 核心规则第 7 条同步更新 |
| `package.json` | 新增 `dev:fresh` 命令 |
| `apps/web/package.json` | `dev` 去掉 `rm -rf .next`，新增 `dev:fresh` |
