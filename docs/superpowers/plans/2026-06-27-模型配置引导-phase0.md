# 模型配置引导 Phase 0 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户首次启动即可在 UI 完成 KEY 配置——`.env` 的 KEY 自动回填到 DB（带"来自 .env"标记），settings 按"对话模型/增强能力"分组，未配置主模型时给出软引导而非静默失败。

**Architecture:** 在现有 `LlmConfig` model 上加 `source` 字段标记来源（user/env）。新增 shared 层回填服务 `backfillFromEnv()`，挂在 server 启动钩子幂等执行。补既有缺口：worker 选 provider 时，无 `llmConfigId` 改为查用户 default 配置再 fallback env。settings 前端按功能分组，主模型未就绪时在入口给软引导。

**Tech Stack:** Prisma（schema + migrate）、Hono（server 路由 + 启动钩子）、Zod（schema 共享）、Vitest（单元测试）、Playwright（E2E）、React 19 + Tailwind 4（前端）、AES-256-GCM（复用现有 `@ai-teacher/shared/services/crypto`）。

---

## 背景与约束（实施前必读）

- **单用户模式**：整个应用用写死的 `userId = "seed-user-ai-teacher"`。本计划**不引入多用户**，所有回填/查询都用这个 userId。
- **加密复用**：`encrypt/decrypt/maskApiKey` 已存在于 `packages/shared/src/services/crypto.ts`，AES-256-GCM，密钥 `LLM_ENCRYPTION_KEY`。**禁止新写加密逻辑**。
- **既有缺口（本计划修复）**：`apps/worker/src/processors/chat-turn.ts:56-74` 的 `getProviderForJob` 仅当传入 `llmConfigId` 才查 DB，否则直接 `getFallbackProvider()` 读 env——不会查用户 default 配置。本计划 Task 6 修复。
- **回填幂等性**：回填只在 DB 无对应配置时插入，不覆盖用户已在 UI 配的。用 `source` 字段区分。
- **文档同步**：本计划涉及 Prisma schema 变更 + 新增 API 端点行为变更，按 `AGENTS.md` 文档同步规则，完成后需同步 `技术架构.md`（数据模型）+ `API接口.md`（如端点语义变化）。Task 9 处理。

---

## 文件结构

**新建：**

- `packages/shared/src/services/llm-backfill.ts` — env→DB 回填核心逻辑（纯函数，可单测）
- `packages/shared/src/services/llm-backfill.test.ts` — 回填单元测试
- `packages/shared/src/services/provider-select.ts` — "查用户 default 配置"选择逻辑（抽自 worker，可单测）
- `packages/shared/src/services/provider-select.test.ts` — 选择逻辑单测

**修改：**

- `packages/db/prisma/schema.prisma:193-208` — `LlmConfig` 加 `source` 字段
- `packages/db/prisma/migrations/<新>/migration.sql` — 迁移（prisma migrate dev 生成）
- `packages/shared/src/schemas/llm-config.ts` — `source` 枚举 + 响应类型加 `source`
- `apps/server/src/index.ts:93-100` — 启动钩子挂回填
- `apps/server/src/routes/llm-config.ts` — GET `/` 和 POST `/` 响应带 `source`；GET `/env-status` 扩展返回回填结果
- `apps/server/src/routes/llm-config.ts` — 新增 POST `/backfill` 手动触发回填（可选，供向导用）
- `apps/worker/src/processors/chat-turn.ts:56-74` — `getProviderForJob` 改用 provider-select
- `apps/web/src/lib/api-client.ts:115-126` — `LlmConfig` 类型加 `source`
- `apps/web/src/pages/settings.tsx` — 按功能分组渲染 + 来源标记
- `apps/web/src/pages/home.tsx` — 主模型未就绪时软引导（复用现有 home）
- `.env.example` — 注释更新
- `docs/设计/技术架构.md` — 数据模型同步
- `e2e/llm-config.spec.ts` — 加回填/分组断言

---

## Task 1: LlmConfig 加 source 字段（DB schema + 迁移）

**Files:**

- Modify: `packages/db/prisma/schema.prisma:193-208`
- Create: `packages/db/prisma/migrations/<timestamp>_add_llm_config_source/migration.sql`

- [ ] **Step 1: 修改 schema 加字段**

在 `packages/db/prisma/schema.prisma` 的 `LlmConfig` model 内，`isDefault` 字段后加：

```prisma
model LlmConfig {
  id           String    @id @default(cuid()) @db.Text
  userId       String    @db.Text
  user         User      @relation(fields: [userId], references: [id])
  provider     String    @db.Text
  encryptedKey String    @db.Text
  baseUrl      String?   @db.Text
  defaultModel String    @db.Text
  label        String?   @db.Text
  isDefault    Boolean   @default(false)
  source       String    @default("user") @db.Text
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  sessions     Session[]

  @@index([userId])
}
```

> `source` 用 `String @default("user")` 而非 Prisma enum——与项目现有约定一致（`Session.status` 等也是 String）。值域：`"user"`（用户在 UI 配）、`"env"`（从 .env 回填）。

- [ ] **Step 2: 生成迁移**

Run: `pnpm --filter @ai-teacher/db exec dotenv -e ../../.env -- prisma migrate dev --name add_llm_config_source`
Expected: 生成 `packages/db/prisma/migrations/<timestamp>_add_llm_config_source/migration.sql`，内容含 `ALTER TABLE "LlmConfig" ADD COLUMN "source" TEXT NOT NULL DEFAULT 'user';`，且 `prisma generate` 成功。

- [ ] **Step 3: 应用迁移并验证**

Run: `pnpm --filter @ai-teacher/db run migrate`
Expected: 迁移 deploy 成功，无报错。

- [ ] **Step 4: 验证现有数据不受影响**

Run: `pnpm --filter @ai-teacher/db exec dotenv -e ../../.env -- prisma studio` （手动）或直接查：

```bash
docker exec -i ai-teacher-db psql -U postgres -d ai_teacher -c "SELECT id, provider, source FROM \"LlmConfig\" LIMIT 5;"
```

Expected: 现有行 `source` 全为 `user`（默认值兜底），无数据丢失。

- [ ] **Step 5: Commit**

```bash
git add packages/db/prisma/schema.prisma packages/db/prisma/migrations/
git commit -m "feat(db): LlmConfig 加 source 字段（user/env 标记来源）"
```

---

## Task 2: shared 层 source 枚举与类型同步

**Files:**

- Modify: `packages/shared/src/schemas/llm-config.ts:8-15,17-28`

- [ ] **Step 1: 加 ConfigSource 枚举 + CreateLlmConfigSchema 加 source**

修改 `packages/shared/src/schemas/llm-config.ts`，在 `ProviderEnum` 定义后加 `ConfigSource`，并给 `CreateLlmConfigSchema` 加可选 `source`，给 `LlmConfigResponse` 加 `source`：

```ts
export const ConfigSource = z.enum(["user", "env"]);

export const CreateLlmConfigSchema = z.object({
  provider: ProviderEnum,
  apiKey: z.string().min(1),
  baseUrl: z.string().url().optional(),
  defaultModel: z.string().min(1),
  label: z.string().optional(),
  isDefault: z.boolean().optional(),
  source: ConfigSource.optional(),
});

export type LlmConfigResponse = {
  id: string;
  userId: string;
  provider: string;
  encryptedKey: string; // masked on read
  baseUrl: string | null;
  defaultModel: string;
  label: string | null;
  isDefault: boolean;
  source: "user" | "env";
  createdAt: string;
  updatedAt: string;
};
```

- [ ] **Step 2: 验证类型编译通过**

Run: `pnpm --filter @ai-teacher/shared run build` （若没有 build 则 `pnpm -F @ai-teacher/shared exec tsc --noEmit`）
Expected: 无类型错误。

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/schemas/llm-config.ts
git commit -m "feat(shared): ConfigSource 枚举 + LlmConfigResponse 加 source"
```

---

## Task 3: llm-backfill 回填核心逻辑（TDD）

**Files:**

- Create: `packages/shared/src/services/llm-backfill.test.ts`
- Create: `packages/shared/src/services/llm-backfill.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/shared/src/services/llm-backfill.test.ts`：

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { backfillLlmConfigsFromEnv } from "./llm-backfill";

// 最小 prisma mock：记录 create 调用，findMany 返回可控
function makeMockPrisma(existing: any[]) {
  const created: any[] = [];
  const updated: any[] = [];
  return {
    created,
    updated,
    prisma: {
      llmConfig: {
        findMany: async () => existing,
        create: async ({ data }: any) => {
          created.push(data);
          return { id: `c${created.length}`, ...data };
        },
        updateMany: async ({ data }: any) => {
          updated.push(data);
          return { count: 0 };
        },
      },
    } as any,
  };
}

describe("backfillLlmConfigsFromEnv", () => {
  beforeEach(() => {
    // 清理 env，每个 case 显式设置
    delete process.env.OPENAI_API_KEY;
    delete process.env.OPENAI_BASE_URL;
  });

  it("env 有 KEY 且 DB 无配置 → 插入 source=env 的配置并设为 default", async () => {
    process.env.OPENAI_API_KEY = "sk-test-123";
    process.env.OPENAI_BASE_URL = "https://api.deepseek.com";
    const { prisma, created } = makeMockPrisma([]);

    const result = await backfillLlmConfigsFromEnv(
      prisma,
      "seed-user-ai-teacher",
    );

    expect(created).toHaveLength(1);
    expect(created[0]).toMatchObject({
      userId: "seed-user-ai-teacher",
      source: "env",
      isDefault: true,
    });
    // provider 推断：baseUrl 含 deepseek → provider "deepseek"
    expect(created[0].provider).toBe("deepseek");
    expect(result.inserted).toBe(1);
  });

  it("DB 已有 user 配置 → 不回填，不覆盖用户配置", async () => {
    process.env.OPENAI_API_KEY = "sk-test-123";
    const { prisma, created } = makeMockPrisma([
      {
        id: "existing",
        userId: "seed-user-ai-teacher",
        provider: "zhipu",
        source: "user",
        isDefault: true,
      },
    ]);

    const result = await backfillLlmConfigsFromEnv(
      prisma,
      "seed-user-ai-teacher",
    );

    expect(created).toHaveLength(0);
    expect(result.inserted).toBe(0);
  });

  it("DB 已有 env 回填配置 → 幂等，不重复插入", async () => {
    process.env.OPENAI_API_KEY = "sk-test-123";
    const { prisma, created } = makeMockPrisma([
      {
        id: "env1",
        userId: "seed-user-ai-teacher",
        provider: "deepseek",
        source: "env",
        isDefault: true,
      },
    ]);

    const result = await backfillLlmConfigsFromEnv(
      prisma,
      "seed-user-ai-teacher",
    );

    expect(created).toHaveLength(0);
    expect(result.inserted).toBe(0);
  });

  it("env 无 KEY → 不回填，返回 inserted=0", async () => {
    const { prisma, created } = makeMockPrisma([]);

    const result = await backfillLlmConfigsFromEnv(
      prisma,
      "seed-user-ai-teacher",
    );

    expect(created).toHaveLength(0);
    expect(result.inserted).toBe(0);
    expect(result.reason).toBe("no-env-key");
  });

  it("baseUrl 不含已知 provider → provider 为 custom", async () => {
    process.env.OPENAI_API_KEY = "sk-test-123";
    process.env.OPENAI_BASE_URL = "https://my-internal-llm.corp/v1";
    const { prisma, created } = makeMockPrisma([]);

    await backfillLlmConfigsFromEnv(prisma, "seed-user-ai-teacher");

    expect(created[0].provider).toBe("custom");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @ai-teacher/shared exec vitest run src/services/llm-backfill.test.ts`
Expected: FAIL — `Cannot find module './llm-backfill'`

- [ ] **Step 3: 实现 llm-backfill.ts**

创建 `packages/shared/src/services/llm-backfill.ts`：

```ts
import { encrypt } from "./crypto";
import type { PrismaClient } from "@prisma/client";

export interface BackfillResult {
  inserted: number;
  reason?:
    | "no-env-key"
    | "user-config-exists"
    | "already-backfilled"
    | "inserted";
}

// baseUrl 关键词 → provider 推断（与 provider-registry 对齐）
function inferProvider(baseUrl: string): string {
  const u = baseUrl.toLowerCase();
  if (u.includes("deepseek")) return "deepseek";
  if (u.includes("dashscope")) return "qianwen";
  if (u.includes("moonshot") || u.includes("kimi")) return "kimi";
  if (u.includes("minimax")) return "minimax";
  if (u.includes("bigmodel")) return "zhipu";
  if (u.includes("openai.com")) return "openai";
  return "custom";
}

/**
 * 从 .env 回填 LLM 配置到 DB。幂等：
 * - DB 已有任意 user 配置 → 不动（用户优先）
 * - DB 已有 env 配置 → 不重复
 * - 否则插入一条 source=env 的默认配置
 */
export async function backfillLlmConfigsFromEnv(
  prisma: PrismaClient,
  userId: string,
): Promise<BackfillResult> {
  const apiKey = process.env.OPENAI_API_KEY;
  if (!apiKey) {
    return { inserted: 0, reason: "no-env-key" };
  }

  const existing = await prisma.llmConfig.findMany({ where: { userId } });

  // 用户已在 UI 配过 → 尊重用户，不回填
  if (existing.some((c) => c.source === "user")) {
    return { inserted: 0, reason: "user-config-exists" };
  }

  // 已有 env 回填记录 → 幂等
  if (existing.some((c) => c.source === "env")) {
    return { inserted: 0, reason: "already-backfilled" };
  }

  const baseUrl = process.env.OPENAI_BASE_URL ?? "";
  const provider = inferProvider(baseUrl);

  await prisma.llmConfig.create({
    data: {
      userId,
      provider,
      encryptedKey: encrypt(apiKey),
      baseUrl: baseUrl || null,
      defaultModel: "deepseek-v4-flash",
      label: "环境变量配置",
      isDefault: true,
      source: "env",
    },
  });

  return { inserted: 1, reason: "inserted" };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm --filter @ai-teacher/shared exec vitest run src/services/llm-backfill.test.ts`
Expected: PASS — 5 个测试全过。

- [ ] **Step 5: Commit**

```bash
git add packages/shared/src/services/llm-backfill.ts packages/shared/src/services/llm-backfill.test.ts
git commit -m "feat(shared): env→DB 回填逻辑 backfillLlmConfigsFromEnv（幂等，带单测）"
```

---

## Task 4: server 启动钩子挂回填

**Files:**

- Modify: `apps/server/src/index.ts:93-100`

- [ ] **Step 1: 读取现有启动钩子确认插入点**

Run: `sed -n '1,30p' apps/server/src/index.ts` 确认 import 区与 listen 回调结构。当前 `index.ts:93-100` 是 `app.listen` 回调，内含 `registerShutdownHook()` → `cleanupOrphanSandboxes()` → `ensureBucket()`。

- [ ] **Step 2: 加 import 和回填调用**

在 `apps/server/src/index.ts` import 区加：

```ts
import { backfillLlmConfigsFromEnv } from "@ai-teacher/shared/services/llm-backfill";
import { prisma } from "@ai-teacher/db";
```

在 `app.listen` 回调里（`ensureBucket()` 之后），追加：

```ts
// 启动时回填 .env 的 LLM KEY 到 DB（幂等，失败不阻塞启动）
backfillLlmConfigsFromEnv(prisma, "seed-user-ai-teacher")
  .then((r) => {
    if (r.inserted > 0)
      console.log(`[backfill] 从 .env 回填了 ${r.inserted} 条 LLM 配置`);
  })
  .catch((err) => console.error("[backfill] 回填失败（不阻塞）:", err));
```

> fire-and-forget，与 `ensureBucket()` 风格一致。失败只 log 不抛，避免加密 key 缺失等场景拖垮启动。

- [ ] **Step 3: 验证启动不报错**

Run: `pnpm --filter @ai-teacher/server exec tsc --noEmit`
Expected: 无类型错误。

- [ ] **Step 4: 手动验证（启动 server，看 log）**

确保 `.env` 有 `OPENAI_API_KEY` 且 `LLM_ENCRYPTION_KEY` 已配。启动：
Run: `pnpm --filter @ai-teacher/server run dev`
Expected: 启动 log 含 `[backfill] 从 .env 回填了 1 条 LLM 配置`（首次）。再次启动 Expected: 无该 log（幂等，`reason=already-backfilled`）。

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/index.ts
git commit -m "feat(server): 启动钩子挂 env→DB 回填（幂等，失败不阻塞）"
```

---

## Task 5: API 响应带 source + 扩展 env-status

**Files:**

- Modify: `apps/server/src/routes/llm-config.ts:52-65`（GET `/` 响应）、`:107-120`（POST `/` 响应）、`:161-174`（PATCH 响应）、`:257-261`（GET `/env-status`）

- [ ] **Step 1: GET `/` 响应加 source**

在 `apps/server/src/routes/llm-config.ts` 的 GET `/`（行 52-65）返回对象里加 `source: cfg.source,`：

```ts
return c.json({
  configs: configs.map((cfg) => ({
    id: cfg.id,
    userId: cfg.userId,
    provider: cfg.provider,
    apiKey: maskApiKey(decrypt(cfg.encryptedKey)),
    baseUrl: cfg.baseUrl,
    defaultModel: cfg.defaultModel,
    label: cfg.label,
    isDefault: cfg.isDefault,
    source: cfg.source,
    createdAt: cfg.createdAt.toISOString(),
    updatedAt: cfg.updatedAt.toISOString(),
  })),
});
```

- [ ] **Step 2: POST `/` 和 PATCH 响应同样加 source**

POST `/`（行 107-120）的返回对象加 `source: config.source,`；PATCH（行 161-174）同样加 `source: config.source,`。三处保持一致。

- [ ] **Step 3: 扩展 GET `/env-status` 返回回填状态**

修改 GET `/env-status`（行 257-261），返回 DB 实际配置情况（前端向导用）：

```ts
.get("/env-status", async (c) => {
  const userId = c.req.query("userId") ?? "seed-user-ai-teacher";
  const hasEnvKey = !!process.env.OPENAI_API_KEY;
  const envBaseUrl = process.env.OPENAI_BASE_URL ?? "";

  const dbConfigs = await prisma.llmConfig.findMany({
    where: { userId },
    select: { id: true, isDefault: true, source: true },
  });

  return c.json({
    hasEnvConfig: hasEnvKey,
    baseUrl: envBaseUrl,
    hasDefaultDbConfig: dbConfigs.some((c) => c.isDefault),
    dbConfigCount: dbConfigs.length,
  });
})
```

- [ ] **Step 4: 类型检查**

Run: `pnpm --filter @ai-teacher/server exec tsc --noEmit`
Expected: 无错误（`cfg.source` 现在是 schema 字段，类型已生效）。

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/routes/llm-config.ts
git commit -m "feat(server): LLM 配置响应带 source + env-status 扩展回填状态"
```

---

## Task 6: provider 选择补"查用户 default 配置"（修复既有缺口）

**Files:**

- Create: `packages/shared/src/services/provider-select.test.ts`
- Create: `packages/shared/src/services/provider-select.ts`
- Modify: `apps/worker/src/processors/chat-turn.ts:56-74`

- [ ] **Step 1: 写失败测试**

创建 `packages/shared/src/services/provider-select.test.ts`：

```ts
import { describe, it, expect } from "vitest";
import { resolveProviderConfig } from "./provider-select";

function mockPrisma(config: any | null) {
  return {
    llmConfig: {
      findFirst: async () => config,
    },
  } as any;
}

describe("resolveProviderConfig", () => {
  it("显式 llmConfigId → 直接查该配置", async () => {
    const prisma = mockPrisma({
      id: "explicit",
      provider: "zhipu",
      encryptedKey: "enc",
      baseUrl: null,
      defaultModel: "glm-5",
    });
    const result = await resolveProviderConfig(prisma, {
      userId: "u1",
      llmConfigId: "explicit",
    });
    expect(result.source).toBe("explicit");
    expect(result.config?.id).toBe("explicit");
  });

  it("无 llmConfigId → 查用户 default 配置", async () => {
    const prisma = mockPrisma({
      id: "def",
      provider: "deepseek",
      encryptedKey: "enc",
      baseUrl: "https://api.deepseek.com",
      defaultModel: "deepseek-v4-flash",
      isDefault: true,
    });
    const result = await resolveProviderConfig(prisma, { userId: "u1" });
    expect(result.source).toBe("default-db");
    expect(result.config?.id).toBe("def");
  });

  it("无 llmConfigId 且 DB 无 default → source=env-fallback", async () => {
    const prisma = mockPrisma(null);
    const result = await resolveProviderConfig(prisma, { userId: "u1" });
    expect(result.source).toBe("env-fallback");
    expect(result.config).toBeNull();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm --filter @ai-teacher/shared exec vitest run src/services/provider-select.test.ts`
Expected: FAIL — `Cannot find module './provider-select'`

- [ ] **Step 3: 实现 provider-select.ts**

创建 `packages/shared/src/services/provider-select.ts`：

```ts
import type { PrismaClient } from "@prisma/client";

export interface RawLlmConfig {
  id: string;
  provider: string;
  encryptedKey: string;
  baseUrl: string | null;
  defaultModel: string;
  isDefault: boolean;
}

export interface ResolveInput {
  userId: string;
  llmConfigId?: string;
}

export type ResolveResult =
  | { source: "explicit"; config: RawLlmConfig }
  | { source: "default-db"; config: RawLlmConfig }
  | { source: "env-fallback"; config: null };

/**
 * 选择 LLM 配置的优先级：
 * 1. 显式 llmConfigId → 查该条
 * 2. 否则查用户 isDefault 配置
 * 3. 都没有 → env-fallback（调用方用 getFallbackProvider）
 */
export async function resolveProviderConfig(
  prisma: PrismaClient,
  input: ResolveInput,
): Promise<ResolveResult> {
  if (input.llmConfigId) {
    const config = await prisma.llmConfig.findFirst({
      where: { id: input.llmConfigId, userId: input.userId },
      select: {
        id: true,
        provider: true,
        encryptedKey: true,
        baseUrl: true,
        defaultModel: true,
        isDefault: true,
      },
    });
    if (!config) throw new Error(`LlmConfig ${input.llmConfigId} not found`);
    return { source: "explicit", config };
  }

  const def = await prisma.llmConfig.findFirst({
    where: { userId: input.userId, isDefault: true },
    select: {
      id: true,
      provider: true,
      encryptedKey: true,
      baseUrl: true,
      defaultModel: true,
      isDefault: true,
    },
  });
  if (def) return { source: "default-db", config: def };

  return { source: "env-fallback", config: null };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm --filter @ai-teacher/shared exec vitest run src/services/provider-select.test.ts`
Expected: PASS — 3 个测试全过。

- [ ] **Step 5: worker 接入 resolveProviderConfig**

修改 `apps/worker/src/processors/chat-turn.ts:56-74` 的 `getProviderForJob`，用 `resolveProviderConfig` 替代原有"无 llmConfigId 直接 fallback"逻辑：

```ts
import { resolveProviderConfig } from "@ai-teacher/shared/services/provider-select";
import { decrypt } from "@ai-teacher/shared/services/crypto";
import {
  createProviderForConfig,
  getFallbackProvider,
} from "@ai-teacher/shared/services/provider-registry";

async function getProviderForJob(llmConfigId?: string): Promise<LlmJobConfig> {
  const resolved = await resolveProviderConfig(prisma, {
    userId: "seed-user-ai-teacher",
    llmConfigId,
  });

  if (resolved.config) {
    const apiKey = decrypt(resolved.config.encryptedKey);
    const providerFn = createProviderForConfig({
      provider: resolved.config.provider,
      apiKey,
      baseUrl: resolved.config.baseUrl ?? undefined,
    });
    return {
      providerFn,
      sandboxModel: resolved.config.defaultModel,
      sandboxBaseUrl: resolved.config.baseUrl ?? undefined,
    };
  }

  return { providerFn: getFallbackProvider() };
}
```

> 注意：保留原 `LlmJobConfig` 返回类型与字段名（`providerFn`/`sandboxModel`/`sandboxBaseUrl`），调用方 `chat-turn.ts:199` 不变。若原函数还有别的字段赋值，对照原实现补齐——执行时先读 chat-turn.ts:56-74 完整代码再改。

- [ ] **Step 6: 验证 worker 类型与行为**

Run: `pnpm --filter @ai-teacher/worker exec tsc --noEmit`
Expected: 无类型错误。

手动验证：确保 DB 有 default 配置时，新会话（不传 llmConfigId）会用 DB 配置而非 env。可在 `getProviderForJob` 临时加 `console.log(resolved.source)` 验证后删除。

- [ ] **Step 7: Commit**

```bash
git add packages/shared/src/services/provider-select.ts packages/shared/src/services/provider-select.test.ts apps/worker/src/processors/chat-turn.ts
git commit -m "fix(worker): provider 选择补「查用户 default 配置」逻辑（修复无 llmConfigId 直读 env 的缺口）"
```

---

## Task 7: 前端类型 + settings 分组与来源标记

**Files:**

- Modify: `apps/web/src/lib/api-client.ts:115-126`
- Modify: `apps/web/src/pages/settings.tsx`

- [ ] **Step 1: 前端 LlmConfig 类型加 source**

修改 `apps/web/src/lib/api-client.ts` 的 `LlmConfig` 类型（行 115-126），加 `source: "user" | "env"`：

```ts
export type LlmConfig = {
  id: string;
  userId: string;
  provider: string;
  apiKey: string;
  baseUrl: string | null;
  defaultModel: string;
  label: string | null;
  isDefault: boolean;
  source: "user" | "env";
  createdAt: string;
  updatedAt: string;
};
```

- [ ] **Step 2: settings 页面给来源配置加标记**

修改 `apps/web/src/pages/settings.tsx`，在配置卡片展示区（行 152-162 附近，`apiKey` 显示之后）加来源 badge：

```tsx
<p className="mt-0.5 text-xs text-muted-foreground">{cfg.apiKey}</p>;
{
  cfg.source === "env" && (
    <span className="mt-1 inline-flex items-center gap-1 rounded-md bg-secondary px-1.5 py-0.5 text-[10px] text-muted-foreground">
      来自 .env
    </span>
  );
}
<p className="mt-0.5 text-xs text-muted-foreground">
  模型：{cfg.defaultModel}
</p>;
```

- [ ] **Step 3: settings 页面按"对话模型/增强能力"分组**

当前所有 `configs` 都是对话模型（LLM）。本 Phase 0 的"增强能力分组"是占位结构——Embedding/视频 KEY 纳入 DB 是后续 Phase 0 扩展点（spec §11.4）。本步先在 LLM 列表上方加分组标题，并在下方加"增强能力（即将支持）"占位卡片：

在 `settings.tsx` 的列表渲染区（`configs.length === 0 ? ... :` 的列表分支，行 122-224），包裹分组标题：

```tsx
) : (
  <div className="space-y-6">
    <div>
      <h2 className="mb-3 text-[11px] font-medium uppercase tracking-widest text-muted-foreground">对话模型（LLM）</h2>
      <div className="space-y-3">
        {configs.map((cfg) => {
          // ... 原有卡片渲染（含上面 Step 2 的来源标记）
        })}
      </div>
    </div>

    <div>
      <h2 className="mb-3 text-[11px] font-medium uppercase tracking-widest text-muted-foreground">增强能力</h2>
      <div className="space-y-3">
        <div className="rounded-xl border border-dashed border-border p-4">
          <p className="text-sm text-muted-foreground">🎬 视频理解 + 🎤 面试语音（DashScope）</p>
          <p className="mt-1 text-xs text-muted-foreground">将在后续迭代支持在 UI 配置</p>
        </div>
        <div className="rounded-xl border border-dashed border-border p-4">
          <p className="text-sm text-muted-foreground">📚 资料 RAG 检索（智谱 Embedding）</p>
          <p className="mt-1 text-xs text-muted-foreground">将在后续迭代支持在 UI 配置</p>
        </div>
      </div>
    </div>
  </div>
)}
```

> 注：增强能力 KEY 的 DB 化（spec §11.4 完整版）涉及新建 ServiceConfig model，超出本 Phase 0 MVP 范围，作为后续子任务。本计划先交付"对话模型完整可用 + 分组结构就位"。

- [ ] **Step 4: 前端类型检查 + 构建**

Run: `pnpm --filter @ai-teacher/web run build`
Expected: 构建成功，无类型错误。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/lib/api-client.ts apps/web/src/pages/settings.tsx
git commit -m "feat(web): LlmConfig 类型加 source + settings 分组与来源标记"
```

---

## Task 8: 主模型未就绪的软引导（home 页 + 配置向导入口）

**Files:**

- Modify: `apps/web/src/pages/home.tsx`

- [ ] **Step 1: 读取 home.tsx 现状**

当前 `apps/web/src/pages/home.tsx` 只是 `useEffect` 重定向到 learn 页（无品牌、无引导）。本步改造成：检测主模型是否就绪，未就绪时显示软引导入口。

- [ ] **Step 2: 加 env-status 检测与软引导**

修改 `apps/web/src/pages/home.tsx`，在重定向逻辑前加配置检测。重写为：

```tsx
import { useEffect, useState } from "react";
import { useNavigate } from "react-router";
import { fetchSessions, getEnvStatus } from "@/lib/api-client";

const USER_ID = "seed-user-ai-teacher";

function generateNewSessionId() {
  const hex = Array.from(globalThis.crypto.getRandomValues(new Uint8Array(16)))
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
  return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-4${hex.slice(13, 16)}-${((parseInt(hex[16], 16) & 0x3) | 0x8).toString(16)}${hex.slice(17, 20)}-${hex.slice(20, 32)}`;
}

export function Component() {
  const navigate = useNavigate();
  const [ready, setReady] = useState<boolean | null>(null);

  useEffect(() => {
    (async () => {
      try {
        const status = await getEnvStatus();
        // 主模型就绪 = DB 有 default 配置 或 env 有 KEY
        setReady(status.hasDefaultDbConfig || status.hasEnvConfig);
      } catch {
        setReady(true); // 检测失败不阻塞，放行让用户进入
      }
    })();
  }, []);

  useEffect(() => {
    if (ready !== true) return;
    fetchSessions(USER_ID)
      .then(({ sessions }) => {
        const learning = sessions.find(
          (s) => s.status === "active" || s.status === "diagnosing",
        );
        if (learning) navigate(`/learn/${learning.id}`, { replace: true });
        else navigate(`/learn/${generateNewSessionId()}`, { replace: true });
      })
      .catch(() =>
        navigate(`/learn/${generateNewSessionId()}`, { replace: true }),
      );
  }, [navigate, ready]);

  if (ready === false) {
    // 软引导：主模型未就绪，提示但不强制
    return (
      <div className="flex h-full items-center justify-center px-6">
        <div className="max-w-md rounded-xl border border-border bg-card p-6 text-center">
          <h1 className="mb-2 text-lg font-semibold text-foreground">
            检测到尚未配置模型
          </h1>
          <p className="mb-4 text-sm text-muted-foreground">
            核心教学功能需要配置 API Key。先去配置，或先看看（部分功能可用）。
          </p>
          <div className="flex justify-center gap-3">
            <button
              onClick={() => navigate("/settings")}
              className="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary-hover"
            >
              去配置
            </button>
            <button
              onClick={() => setReady(true)}
              className="rounded-lg border border-border px-4 py-2 text-sm text-muted-foreground hover:bg-secondary"
            >
              先进入
            </button>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="flex h-full items-center justify-center">
      <div className="flex flex-col items-center gap-3">
        <div className="h-8 w-8 animate-pulse-soft rounded-full bg-roadmap-fill" />
      </div>
    </div>
  );
}
```

> 关键：`setReady(true)` 让"先进入"放行（软引导，不强制）。配置失败的兜底是 `catch { setReady(true) }`，避免检测故障导致用户卡死。

- [ ] **Step 3: 验证 getEnvStatus 返回类型已含 hasDefaultDbConfig**

确认 `apps/web/src/lib/api-client.ts:206` 的 `getEnvStatus` 返回类型——Task 5 Step 3 已扩展 server 返回 `hasDefaultDbConfig`，前端类型需同步：

修改 `getEnvStatus` 返回类型：

```ts
export async function getEnvStatus(): Promise<{
  hasEnvConfig: boolean;
  baseUrl: string;
  hasDefaultDbConfig: boolean;
  dbConfigCount: number;
}> {
  // ... 现有实现
}
```

- [ ] **Step 4: 前端构建验证**

Run: `pnpm --filter @ai-teacher/web run build`
Expected: 构建成功。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/pages/home.tsx apps/web/src/lib/api-client.ts
git commit -m "feat(web): home 页主模型未就绪软引导（不强制，可跳过进入）"
```

---

## Task 9: 文档同步（.env.example + 技术架构）

**Files:**

- Modify: `.env.example:17-31, 48-50`
- Modify: `docs/设计/技术架构.md`（数据模型章节）

- [ ] **Step 1: 更新 .env.example 注释**

修改 `.env.example`，给 LLM 相关 KEY 加"会自动回填到 UI"的说明：

```env
# LLM 主对话模型（DeepSeek OpenAI 兼容接口）
# ⚠️ 首次启动时会自动回填到「模型设置」UI，之后在 UI 修改以 UI 为准
OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.deepseek.com
```

`LLM_ENCRYPTION_KEY` 注释补充：

```env
# LLM API Key 加密密钥 (32 字节 hex，用于加密用户 API Key)
# ⚠️ 必填！未配置则回填和加密都会失败
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
LLM_ENCRYPTION_KEY=
```

- [ ] **Step 2: 同步技术架构.md 数据模型**

在 `docs/设计/技术架构.md` 的数据模型章节，找到 `LlmConfig` 的 prisma 代码块，更新为含 `source` 字段的版本（与 Task 1 Step 1 的 schema 一致）。

Run: 搜索 `model LlmConfig` 定位，替换整块为最新 schema。

- [ ] **Step 3: 自检文档一致性**

按 `文档同步规则.md` 自检方法：Prisma Schema ↔ 文档中的 prisma 代码块完全一致。

Run: `diff <(sed -n '/model LlmConfig/,/^}/p' packages/db/prisma/schema.prisma) <(sed -n '/model LlmConfig/,/^}/p' docs/设计/技术架构.md)`
Expected: 无差异（或仅空白差异）。

- [ ] **Step 4: Commit**

```bash
git add .env.example docs/设计/技术架构.md
git commit -m "docs: .env.example 回填说明 + 技术架构 LlmConfig schema 同步 source 字段"
```

---

## Task 10: E2E 测试加回填/分组断言

**Files:**

- Modify: `e2e/llm-config.spec.ts`
- Modify: `e2e/global-setup.ts`（若需）

- [ ] **Step 1: 读取现有 llm-config.spec.ts 与 global-setup**

Run: `sed -n '1,30p' e2e/llm-config.spec.ts` 和 `sed -n '1,30p' e2e/global-setup.ts`。
确认 global-setup 是否 drop+recreate 测试库（影响回填测试的 env 设置）。

- [ ] **Step 2: 加 API 层回填行为断言**

在 `e2e/llm-config.spec.ts` 末尾加 describe：

```ts
import { test, expect } from "./fixtures";

describe("LLM Config — backfill & source", () => {
  test("GET / 返回的配置含 source 字段", async ({ request }) => {
    // 前提：global-setup 已触发回填（env 有 OPENAI_API_KEY）
    const res = await request.get(`/api/llm?userId=seed-user-ai-teacher`);
    const body = await res.json();
    for (const cfg of body.configs) {
      expect(["user", "env"]).toContain(cfg.source);
    }
  });

  test("GET /env-status 返回回填状态字段", async ({ request }) => {
    const res = await request.get(`/api/llm/env-status`);
    const body = await res.json();
    expect(body).toHaveProperty("hasEnvConfig");
    expect(body).toHaveProperty("hasDefaultDbConfig");
    expect(body).toHaveProperty("dbConfigCount");
    expect(typeof body.dbConfigCount).toBe("number");
  });

  test("POST 创建的配置 source 默认 user", async ({ request }) => {
    const res = await request.post(`/api/llm?userId=seed-user-ai-teacher`, {
      data: {
        provider: "deepseek",
        apiKey: "sk-e2e-source-test",
        baseUrl: "https://api.deepseek.com",
        defaultModel: "deepseek-v4-flash",
      },
    });
    const body = await res.json();
    expect(body.config.source).toBe("user");
    // 清理
    await request.delete(
      `/api/llm/${body.config.id}?userId=seed-user-ai-teacher`,
    );
  });
});
```

> 注意：回填的"env→DB 实际插入"行为依赖 server 启动，E2E 难以精确断言"启动时插入了"。上述断言聚焦"API 响应字段正确性"和"env-status 扩展字段"，回填逻辑本身由 Task 3 的单测覆盖。

- [ ] **Step 3: 运行 E2E 验证**

Run: `pnpm test:e2e e2e/llm-config.spec.ts`
Expected: 全部通过（含新增 3 个 case）。

- [ ] **Step 4: Commit**

```bash
git add e2e/llm-config.spec.ts
git commit -m "test(e2e): LLM 配置回填/source 字段断言 + env-status 扩展字段"
```

---

## Task 11: 人工验收走查 + 更新开发日志

**Files:**

- Modify: `docs/开发/开发日志.md`

- [ ] **Step 1: 按 spec §9.2 Phase 0 验收清单走查**

逐项确认（人工）：

- [ ] 首次启动（DB 无配置）+ env 有 KEY → 自动回填，settings 显示"来自 .env"标记
- [ ] 主 LLM 未配置（无 env KEY 且 DB 空）→ home 页软引导，可跳过进入
- [ ] settings 页"对话模型/增强能力"分组渲染正确
- [ ] 回填幂等：重启 server 不重复插入
- [ ] 回填不覆盖：先在 UI 配一个 user 配置，再重启，env 不回填
- [ ] 删除 env 回填的配置后重启 → 重新回填（用户未配 user 配置时）

- [ ] **Step 2: 更新开发日志**

在 `docs/开发/开发日志.md` 加本次迭代记录，并在"下次继续"区域写明 Phase 0 已完成、下一步是 Phase 1。

- [ ] **Step 3: 更新迭代计划状态**

修改 `docs/开发/迭代计划.md`，把 049 状态从 `⬜ 待开始` 改为 `✅ 已完成` + 日期。

- [ ] **Step 4: Commit**

```bash
git add docs/开发/开发日志.md docs/开发/迭代计划.md
git commit -m "docs: Phase 0 模型配置引导完成 — 开发日志 + 迭代计划状态更新"
```

---

## Self-Review 检查（plan 完成后自检）

**1. Spec 覆盖**（对照 spec §11 和 §9.2 Phase 0）：

- §11.2 ".env 回填 DB" → Task 1-4 ✓
- §11.2 "Embedding/视频 KEY 纳入 DB" → Task 7 Step 3 占位结构，**完整 DB 化标记为后续子任务**（计划内已说明，非缺失）
- §11.3 "首次配置向导（软引导）" → Task 8 ✓
- §11.4 "settings 分组" → Task 7 ✓
- §11.5 "技术要点（回填幂等/.env.example 同步/加密）" → Task 3/9 ✓
- §9.2 Phase 0 验收 → Task 10-11 ✓
- §11.5 提到"修复 provider 选择缺口"（实际调研发现）→ Task 6 ✓

**2. 类型一致性**：

- `source` 字段在 schema(Task1) → shared schema(Task2) → server 响应(Task5) → 前端类型(Task7) → E2E(Task10) 全链路一致 ✓
- `resolveProviderConfig` 在 Task6 定义，worker 接入字段名 `providerFn/sandboxModel/sandboxBaseUrl` 与原 `LlmJobConfig` 对齐 ✓
- `getEnvStatus` 返回类型 Task5(Task5扩展server) ↔ Task8(前端消费) 一致 ✓

**3. 已知范围限制（计划内已标注，非缺陷）**：

- 增强能力 KEY（Embedding/DashScope）的完整 DB 化（新建 ServiceConfig model）超出本 MVP，Task 7 只做分组占位。
- 配置向导的"分步卡片 UI"（spec §10.3 那种多步向导）本计划用 home 页软引导简化交付，完整向导 UI 留后续。

---

## 执行交接

Plan complete and saved to `docs/superpowers/plans/2026-06-27-模型配置引导-phase0.md`.
